export const dynamic='force-dynamic';
import { Nav } from '@/components/nav'; import { requireMember } from '@/lib/auth'; import { createSupabaseAdminClient } from '@/lib/supabase-admin'; import { SupportForm } from './support-form';
export default async function Support(){const {user,membership}=await requireMember(); const sb=createSupabaseAdminClient(); const {data=[]}=await sb.from('dp_support_tickets').select('*').eq('reporter_id',user.id).order('created_at',{ascending:false}).limit(20); return <><Nav admin={membership.role==='admin'} email={membership.email}/><main className="mx-auto max-w-5xl p-8"><h1 className="text-3xl font-semibold">Support</h1><p className="mt-2 text-slate-500">Report bugs, ask for help, or send content feedback.</p><div className="mt-6 grid gap-6 md:grid-cols-[1fr_320px]"><SupportForm/><aside className="rounded-2xl border bg-white p-5"><h2 className="font-semibold">Contact support</h2><p className="mt-2 text-sm text-slate-500">{process.env.SUPPORT_EMAIL||'Use this form and the DP Resources team will review your ticket.'}</p></aside></div><h2 className="mt-8 text-xl font-semibold">Your tickets</h2><div className="mt-3 rounded-xl border bg-white">{(data as any[]).map(t=><div key={t.id} className="border-b p-4"><b>{t.subject}</b><span className="ml-2 text-xs uppercase text-slate-500">{t.status}</span><p className="text-sm text-slate-500">{t.category}</p></div>)}</div></main></>}

'use client';
import { useState } from 'react';
export function SupportForm(){const [ok,setOk]=useState(false); async function submit(e:any){e.preventDefault(); const fd=new FormData(e.currentTarget); await fetch('/api/support',{method:'POST',body:JSON.stringify(Object.fromEntries(fd)),headers:{'content-type':'application/json'}}); setOk(true); e.currentTarget.reset();} return <form onSubmit={submit} className="rounded-2xl border bg-white p-5 shadow-sm"><select name="category" className="mb-3 w-full rounded-xl border p-3"><option>Report a bug</option><option>Suggest an improvement</option><option>Ask for help</option><option>Account issue</option><option>Content feedback</option><option>Other</option></select><input name="subject" required placeholder="Subject" className="mb-3 w-full rounded-xl border p-3"/><textarea name="message" required rows={7} placeholder="Detailed message" className="mb-3 w-full rounded-xl border p-3"/><button className="rounded-xl bg-slate-950 px-4 py-2 text-white">Submit ticket</button>{ok&&<p className="mt-3 text-sm text-emerald-700">Ticket submitted.</p>}</form>}

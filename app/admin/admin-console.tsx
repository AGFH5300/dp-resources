'use client';
import Link from 'next/link';
import { usePathname, useRouter } from 'next/navigation';
import { useEffect, useMemo, useState } from 'react';
import { toast } from 'sonner';
import { AppSelect as AdminSelect } from '@/components/ui/app-select';
import { EmailSearchInput } from '@/components/ui/email-search-input';
import type { ActivityLog, ResourceMembership, ResourceReport, SupportTicket } from '@/lib/types';

const statuses = ['open', 'in_review', 'resolved', 'closed'];
const statusOptions = [{ value: '', label: 'Any status' }, ...statuses.map(s => ({ value: s, label: s.replace('_', ' ') }))];
const caseStatusOptions = statuses.map(s => ({ value: s, label: s.replace('_', ' ') }));
const pageSizeOptions = ['25', '50', '100'].map(v => ({ value: v, label: v }));
const inputClass = 'mt-1 h-9 w-full rounded-md border border-slate-300 bg-[color:var(--dp-warm-surface)] px-3 text-sm text-slate-800 outline-none focus-visible:border-slate-400 focus-visible:bg-white';
const secondaryBtn = 'rounded-md border border-slate-300 bg-white px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50 disabled:opacity-60';
const primaryBtn = 'rounded-md border border-[color:var(--dp-blue)] bg-[color:var(--dp-blue)] px-3 py-2 text-sm font-medium text-white hover:opacity-90 disabled:opacity-60';

function badge(s: string) { const m: Record<string, string> = { open: 'bg-amber-50 text-amber-800 border-amber-200', in_review: 'bg-indigo-50 text-indigo-800 border-indigo-200', resolved: 'bg-emerald-50 text-emerald-800 border-emerald-200', closed: 'bg-slate-100 text-slate-700 border-slate-200' }; return <span className={`rounded border px-2 py-0.5 text-xs font-medium capitalize ${m[s] || m.closed}`}>{s.replace('_', ' ')}</span>; }
function sectionHref(sp: Record<string, string | undefined>, section: string) { return { query: { ...sp, section } }; }
function pager(base: Record<string, string | undefined>, key: string, page: number, total: number, per: number) { const pages = Math.max(1, Math.ceil(total / per)); return <div className="flex items-center justify-end gap-2 p-2 text-xs text-slate-500"><span>Page {page} of {pages}</span>{page > 1 && <Link href={{ query: { ...base, [key]: String(page - 1) } }} className={secondaryBtn}>Previous</Link>}{page < pages && <Link href={{ query: { ...base, [key]: String(page + 1) } }} className={secondaryBtn}>Next</Link>}</div>; }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-xs font-semibold text-slate-600">{label}{children}</label>; }
function useLiveParams(sp: Record<string, string | undefined>, section: string) { const router = useRouter(); const pathname = usePathname(); return (patch: Record<string, string>, resetKeys: string[]) => { const q = new URLSearchParams(); Object.entries({ ...sp, section }).forEach(([k, v]) => { if (v) q.set(k, v); }); Object.entries(patch).forEach(([k, v]) => v ? q.set(k, v) : q.delete(k)); resetKeys.forEach(k => q.set(k, '1')); router.replace(`${pathname}?${q.toString()}`); }; }
function LiveFilters({ prefix, sp, section }: { prefix: string; sp: Record<string, string | undefined>; section: string }) { const apply = useLiveParams(sp, section); const [email, setEmail] = useState(sp[`${prefix}Email`] || ''), [search, setSearch] = useState(sp[`${prefix}Search`] || ''); useEffect(() => { const t = setTimeout(() => apply({ [`${prefix}Email`]: email, [`${prefix}Search`]: search }, [`${prefix}Page`]), 300); return () => clearTimeout(t); }, [email, search]); return <div className="mb-3 mt-3 grid gap-3 md:grid-cols-6"><Field label="Status"><AdminSelect value={sp[`${prefix}Status`] || ''} onValueChange={v => apply({ [`${prefix}Status`]: v }, [`${prefix}Page`])} options={statusOptions} /></Field><EmailSearchInput label="Reporter email" value={email} onChange={setEmail} /><Field label={prefix === 'report' ? 'Resource / path' : 'Subject'}><input value={search} onChange={e => setSearch(e.target.value)} className={inputClass} /></Field><Field label="From date"><input type="date" value={sp[`${prefix}From`] || ''} onChange={e => apply({ [`${prefix}From`]: e.target.value }, [`${prefix}Page`])} className={inputClass} /></Field><Field label="To date"><input type="date" value={sp[`${prefix}To`] || ''} onChange={e => apply({ [`${prefix}To`]: e.target.value }, [`${prefix}Page`])} className={inputClass} /></Field><button type="button" onClick={() => apply({ [`${prefix}Status`]: '', [`${prefix}Email`]: '', [`${prefix}Search`]: '', [`${prefix}From`]: '', [`${prefix}To`]: '' }, [`${prefix}Page`])} className="self-end text-left text-sm font-medium text-[color:var(--dp-blue)]">Clear filters</button></div>; }
function UserFilters({ sp, sizes }: { sp: Record<string, string | undefined>; sizes: any }) { const apply = useLiveParams(sp, 'users'); const [email, setEmail] = useState(sp.userEmail || ''); useEffect(() => { const t = setTimeout(() => apply({ userEmail: email }, ['userPage']), 300); return () => clearTimeout(t); }, [email]); return <div className="mt-3 grid gap-3 md:grid-cols-4"><EmailSearchInput label="User email" value={email} onChange={setEmail} /><Field label="Role"><AdminSelect value={sp.role || ''} onValueChange={v => apply({ role: v }, ['userPage'])} options={[{ value: '', label: 'Any role' }, { value: 'user', label: 'user' }, { value: 'admin', label: 'admin' }]} /></Field><Field label="Results per page"><AdminSelect value={String(sizes.user)} onValueChange={v => apply({ userSize: v }, ['userPage'])} options={pageSizeOptions} /></Field><button type="button" onClick={() => apply({ userEmail: '', role: '', userSize: '' }, ['userPage'])} className="self-end text-left text-sm font-medium text-[color:var(--dp-blue)]">Clear filters</button></div>; }
function ActivityFilters({ sp }: { sp: Record<string, string | undefined> }) { const apply = useLiveParams(sp, 'activity'); const [email, setEmail] = useState(sp.email || ''), [file, setFile] = useState(sp.file || ''); useEffect(() => { const t = setTimeout(() => apply({ email, file }, ['activityPage']), 300); return () => clearTimeout(t); }, [email, file]); return <div className="mt-3 grid gap-3 md:grid-cols-6"><EmailSearchInput label="User email" value={email} onChange={setEmail} /><Field label="Resource"><input value={file} onChange={e => setFile(e.target.value)} className={inputClass} /></Field><Field label="Activity"><AdminSelect value={sp.action || ''} onValueChange={v => apply({ action: v }, ['activityPage'])} options={[{ value: '', label: 'Any activity' }, { value: 'folder_opened', label: 'Opened folder' }, { value: 'file_opened', label: 'Opened file' }, { value: 'download_started', label: 'Started download' }]} /></Field><Field label="From date"><input type="date" value={sp.from || ''} onChange={e => apply({ from: e.target.value }, ['activityPage'])} className={inputClass} /></Field><Field label="To date"><input type="date" value={sp.to || ''} onChange={e => apply({ to: e.target.value }, ['activityPage'])} className={inputClass} /></Field><button type="button" onClick={() => apply({ email: '', file: '', action: '', from: '', to: '' }, ['activityPage'])} className="self-end text-left text-sm font-medium text-[color:var(--dp-blue)]">Clear filters</button></div>; }
function CaseInspector({ kind, item, admins, onClose }: { kind: 'report' | 'ticket'; item: ResourceReport | SupportTicket; admins: ResourceMembership[]; onClose: () => void }) { const router = useRouter(); const isReport = kind === 'report'; const [draft, setDraft] = useState<any>({ status: item.status, assigned_to: (item as any).assigned_to || '', internal_notes: (item as any).internal_notes || '', ...(isReport ? { resolution_note: (item as ResourceReport).resolution_note || '' } : {}) }); const [saving, setSaving] = useState(false), [err, setErr] = useState(''); const [reply,setReply]=useState(''); const [messages,setMessages]=useState<any[]>((item as any).messages || []); async function save(final?: string) { setSaving(true); setErr(''); const status = final || draft.status; const res = await fetch(`/api/admin/${kind === 'report' ? 'reports' : 'support'}/${item.id}`, { method:'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ ...draft, status, assigned_to: draft.assigned_to || null }) }); const json = await res.json().catch(() => ({ error: 'Could not update case' })); setSaving(false); if (!res.ok) { setErr(json.error || 'Could not update case'); toast.error(kind === 'report' ? 'Could not update report' : 'Could not update support ticket'); return; } toast.success(kind === 'report' ? 'Report updated' : 'Support ticket updated'); setDraft({ ...draft, status }); router.refresh(); }
 async function sendReply(visibility:'user'|'internal'='user') { if (!reply.trim()) return; setSaving(true); setErr(''); const res = await fetch(`/api/admin/support/${item.id}/messages`, { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ body: reply, visibility }) }); const json = await res.json().catch(() => ({ error: 'Could not send reply' })); setSaving(false); if (!res.ok) { setErr(json.error || 'Could not send reply'); toast.error('Could not send reply'); return; } setMessages(prev => [...prev, json.message]); setReply(''); toast.success(visibility === 'user' ? 'Reply sent' : 'Internal note saved'); router.refresh(); }
 return <aside className="fixed inset-y-0 right-0 z-40 w-full overflow-auto border-l border-slate-200 bg-white p-5 shadow-xl md:top-[72px] md:w-[420px]"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{isReport ? 'Resource report' : 'Support ticket'}</p><h3 className="text-lg font-semibold text-[color:var(--dp-navy)]">{isReport ? ((item as ResourceReport).resource_name || 'Resource report') : (item as SupportTicket).subject}</h3></div><button onClick={onClose} className={secondaryBtn}>Close</button></div><dl className="mt-4 space-y-3 text-sm"><div><dt className="font-medium">{isReport ? 'Resource path' : 'Category'}</dt><dd className="text-slate-600">{isReport ? ((item as ResourceReport).resource_path || '—') : (item as SupportTicket).category}</dd></div><div><dt className="font-medium">Reporter</dt><dd className="text-slate-600">{item.reporter_email}</dd></div><div><dt className="font-medium">Submitted</dt><dd className="text-slate-600">{new Date(item.created_at).toLocaleString()}</dd></div><div><dt className="font-medium">Assigned admin</dt><dd className="text-slate-600">{(item as any).assigned_to ? admins.find(a=>a.id===(item as any).assigned_to)?.email || 'Assigned' : 'Unassigned'}</dd></div><div><dt className="font-medium">Original message</dt><dd className="whitespace-pre-wrap rounded-md bg-slate-50 p-3 text-slate-700">{item.message}</dd></div></dl>{!isReport && <section className="mt-4"><h4 className="text-sm font-semibold">Visible conversation</h4><div className="mt-2 space-y-2">{messages.filter(m=>m.visibility!=='internal').map(m=><div key={m.id} className="rounded-md border border-blue-100 bg-blue-50 p-3 text-sm"><b>Admin reply</b><p className="whitespace-pre-wrap">{m.body}</p></div>)}</div></section>}{isReport && (item as ResourceReport).drive_file_id && <div className="mt-4"><Link target="_blank" href={`/resource/${(item as ResourceReport).drive_file_id}`} className={secondaryBtn}>Open resource in new tab</Link></div>}<div className="mt-4 grid gap-3"><Field label="Status"><AdminSelect disabled={saving} value={draft.status} onValueChange={v => setDraft({ ...draft, status: v })} options={caseStatusOptions} /></Field><Field label="Assign to"><AdminSelect disabled={saving} value={draft.assigned_to || 'unassigned'} onValueChange={v => setDraft({ ...draft, assigned_to: v === 'unassigned' ? '' : v })} options={[{ value: 'unassigned', label: 'Unassigned' }, ...admins.map(a => ({ value: a.id, label: a.email }))]} /></Field><label className="text-xs font-semibold text-slate-600">Internal notes<textarea disabled={saving} value={draft.internal_notes} onChange={e => setDraft({ ...draft, internal_notes: e.target.value })} className={`${inputClass} min-h-24 py-2`} /></label>{isReport && <label className="text-xs font-semibold text-slate-600">Resolution note<textarea disabled={saving} value={draft.resolution_note} onChange={e => setDraft({ ...draft, resolution_note: e.target.value })} className={`${inputClass} min-h-20 py-2`} /></label>}{!isReport && <label className="text-xs font-semibold text-slate-600">User-visible reply<textarea disabled={saving} value={reply} onChange={e => setReply(e.target.value)} className={`${inputClass} min-h-20 py-2`} /></label>}{err && <p className="rounded-md bg-red-50 p-2 text-sm text-red-700">{err}</p>}<div className="flex flex-wrap gap-2">{!isReport && <button disabled={saving || !reply.trim()} onClick={() => sendReply('user')} className={primaryBtn}>Send reply</button>}<button disabled={saving} onClick={() => save()} className={primaryBtn}>Save changes</button><button disabled={saving} onClick={() => save('in_review')} className={secondaryBtn}>Mark in review</button><button disabled={saving} onClick={() => save('resolved')} className={secondaryBtn}>Mark resolved</button><button disabled={saving} onClick={() => save('closed')} className="rounded-md border border-red-200 bg-red-50 px-3 py-2 text-sm font-medium text-red-700 disabled:opacity-60">Close case</button></div></div></aside>; }
export function AdminConsole({ sp, reports, tickets, memberships, logs, counts, pages, sizes, indexPanel, configuredWarnings, exportQuery }: { sp: Record<string, string | undefined>; reports: ResourceReport[]; tickets: SupportTicket[]; memberships: ResourceMembership[]; logs: ActivityLog[]; counts: any; pages: any; sizes: any; indexPanel: React.ReactNode; configuredWarnings: React.ReactNode; exportQuery: string }) { const section = sp.section || 'index'; const [selected, setSelected] = useState<{ kind: 'report' | 'ticket'; item: any } | null>(null); const admins = useMemo(() => memberships.filter(m => m.role === 'admin'), [memberships]); const tabs = [['index', 'Library index'], ['reports', 'Resource reports'], ['tickets', 'Support tickets'], ['users', 'Users'], ['activity', 'Activity']]; const activityLabel = (a: string) => ({ folder_opened: 'Opened folder', file_opened: 'Opened file', download_started: 'Started download' }[a] || a); return <><nav className="mt-4 flex flex-wrap gap-2 border-b border-slate-200 text-sm" aria-label="Admin sections">{tabs.map(([id, label]) => <Link key={id} href={sectionHref(sp, id)} className={`rounded-t-md px-3 py-2 ${section === id ? 'bg-slate-100 font-semibold text-[color:var(--dp-navy)]' : 'text-slate-600 hover:bg-slate-50'}`}>{label}</Link>)}</nav>{configuredWarnings}{section === 'index' && <section className="mt-5">{indexPanel}</section>}{section === 'reports' && <section className="mt-5"><h2 className="text-base font-semibold">Resource reports</h2><LiveFilters prefix="report" sp={sp} section="reports" /><QueueTable rows={reports} kind="report" onInspect={item => setSelected({ kind: 'report', item })} />{pager(sp, 'reportPage', pages.report, counts.report, sizes.page)}</section>}{section === 'tickets' && <section className="mt-5"><h2 className="text-base font-semibold">Support tickets</h2><LiveFilters prefix="ticket" sp={sp} section="tickets" /><QueueTable rows={tickets} kind="ticket" onInspect={item => setSelected({ kind: 'ticket', item })} />{pager(sp, 'ticketPage', pages.ticket, counts.ticket, sizes.page)}</section>}{section === 'users' && <section className="mt-5"><h2 className="text-base font-semibold">Users</h2><UserFilters sp={sp} sizes={sizes} /><div className="mt-3 overflow-x-auto border-y border-slate-200 bg-white"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-2">User</th><th className="p-2">Role</th><th className="p-2">Joined</th><th className="p-2">Last activity</th></tr></thead><tbody>{memberships.map(u => <tr key={u.id} className="border-t border-slate-100"><td className="p-2 font-medium">{u.email}</td><td className="p-2">{u.role}</td><td className="p-2 text-slate-500">{new Date(u.created_at).toLocaleDateString()}</td><td className="p-2 text-slate-500">{(u as any).latest_activity_at ? new Date((u as any).latest_activity_at).toLocaleString() : '—'}</td></tr>)}</tbody></table>{pager(sp, 'userPage', pages.user, counts.user, sizes.user)}</div></section>}{section === 'activity' && <section className="mt-5"><h2 className="text-base font-semibold">Activity</h2><ActivityFilters sp={sp} /><a className="mt-3 inline-block text-sm font-medium text-[color:var(--dp-blue)]" href={`/api/admin/activity/export?${exportQuery}`}>Export CSV</a><div className="mt-3 overflow-x-auto border-y border-slate-200 bg-white"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-2">Time</th><th className="p-2">User</th><th className="p-2">Activity</th><th className="p-2">Resource</th></tr></thead><tbody>{logs.map(log => <tr key={log.id} className="border-t border-slate-100"><td className="p-2 text-slate-500">{new Date(log.created_at).toLocaleString()}</td><td className="p-2 font-medium">{log.user_email}</td><td className="p-2">{activityLabel(log.action)}</td><td className="p-2">{log.file_name || 'Resource'}</td></tr>)}</tbody></table>{pager(sp, 'activityPage', pages.activity, counts.activity, 50)}</div></section>}{selected && <CaseInspector kind={selected.kind} item={selected.item} admins={admins} onClose={() => setSelected(null)} />}</>; }
function QueueTable({ rows, kind, onInspect }: { rows: any[]; kind: 'report' | 'ticket'; onInspect: (item: any) => void }) { return <div className="overflow-x-auto border-y border-slate-200 bg-white"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-2">Status</th><th className="p-2">Category</th><th className="p-2">{kind === 'report' ? 'Resource' : 'Subject'}</th><th className="p-2">Reporter</th><th className="p-2">Submitted</th><th className="p-2">Assigned</th><th className="p-2">Action</th></tr></thead><tbody>{rows.map(r => <tr key={r.id} onClick={() => onInspect(r)} className="cursor-pointer border-t border-slate-100 hover:bg-slate-50"><td className="p-2">{badge(r.status)}</td><td className="p-2">{r.category}</td><td className="max-w-72 truncate p-2">{kind === 'report' ? r.resource_name : r.subject}<p className="text-xs text-slate-500">{kind === 'report' ? r.resource_path : ''}</p></td><td className="p-2">{r.reporter_email}</td><td className="p-2">{new Date(r.created_at).toLocaleString()}</td><td className="p-2">{r.assigned_to ? 'Assigned' : 'Unassigned'}</td><td className="p-2"><button onClick={e => { e.stopPropagation(); onInspect(r); }} className="font-medium text-[color:var(--dp-blue)]">Inspect</button></td></tr>)}</tbody></table></div>; }

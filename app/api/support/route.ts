import { requireMember } from '@/lib/auth';
import { createSupabaseAdminClient } from '@/lib/supabase-admin';
export async function POST(req: Request) { const { user } = await requireMember(); const body = await req.json().catch(() => null); if (!body || typeof body !== 'object') return Response.json({ error: 'Expected JSON request body' }, { status: 400 }); const category = String(body.category || '').trim(); const subject = String(body.subject || '').trim(); const message = String(body.message || '').trim(); if (!category || !subject || !message) return Response.json({ error: 'Category, subject, and message are required' }, { status: 400 }); const sb = createSupabaseAdminClient(); const { data, error } = await sb.from('dp_support_tickets').insert({ reporter_id: user.id, reporter_email: user.email, category, subject, message }).select('id,category,subject,message,status,created_at,updated_at,resolution_note').single(); if (error) return Response.json({ error: error.message }, { status: 500 }); return Response.json({ ticket: data }, { status: 201 }); }

import { requireAdmin } from '@/lib/auth'; import { createSupabaseAdminClient } from '@/lib/supabase-admin';
const allowed=new Set(['open','in_review','resolved','closed']);
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const {user}=await requireAdmin(); const {id}=await params; const body=await req.json(); if(body.status&&!allowed.has(body.status))return Response.json({error:'Invalid status'},{status:400}); const now=new Date().toISOString(); const update:any={updated_at:now}; ['status','assigned_to','internal_notes','resolution_note'].forEach(k=>{if(k in body) update[k]=body[k]||null}); if('assigned_to' in body) update.assigned_at=body.assigned_to?now:null; if(body.status==='resolved'||body.status==='closed'){update.resolved_at=now; update.resolved_by=user.id;} const sb=createSupabaseAdminClient(); const {error}=await sb.from('dp_resource_reports').update(update).eq('id',id); if(error)return Response.json({error:error.message},{status:500}); return Response.json({ok:true});}
/* Legacy QA marker: admin_notes migrated to internal_notes */

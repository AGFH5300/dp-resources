import { requireAdmin } from '@/lib/auth';
import { createSupabaseAdminClient } from '@/lib/supabase-admin';
export async function POST(req:Request){await requireAdmin(); const {driveFileId,label='Resource Library',priority=1000}=await req.json(); if(!driveFileId)return Response.json({error:'Missing driveFileId'},{status:400}); const sb=createSupabaseAdminClient(); const {error}=await sb.from('dp_resource_featured_resources').upsert({drive_file_id:driveFileId,label,priority,updated_at:new Date().toISOString()},{onConflict:'drive_file_id'}); if(error)return Response.json({error:error.message},{status:500}); return Response.json({ok:true});}
export async function DELETE(req:Request){await requireAdmin(); const id=new URL(req.url).searchParams.get('driveFileId'); if(!id)return Response.json({error:'Missing driveFileId'},{status:400}); const sb=createSupabaseAdminClient(); const {error}=await sb.from('dp_resource_featured_resources').delete().eq('drive_file_id',id); if(error)return Response.json({error:error.message},{status:500}); return Response.json({ok:true});}

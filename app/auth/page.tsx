import { login, signup } from './actions';
export default async function Auth({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){const sp=await searchParams; const mode=sp.mode==='signup'?'signup':'login'; return <main className="flex min-h-screen items-center justify-center px-4"><div className="w-full max-w-md rounded-2xl border bg-white p-8 shadow-sm"><h1 className="text-center text-2xl font-semibold">DP Resources</h1><div className="mt-6 grid grid-cols-2 rounded-lg bg-slate-100 p-1 text-center text-sm"><a className={`rounded-md p-2 ${mode==='login'?'bg-white shadow-sm':''}`} href="/auth">Login</a><a className={`rounded-md p-2 ${mode==='signup'?'bg-white shadow-sm':''}`} href="/auth?mode=signup">Sign up</a></div>{sp.verify&&<p className="mt-4 rounded-lg bg-green-50 p-3 text-sm text-green-800">Check your email to verify your account. After verification, an admin must approve access.</p>}{sp.error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{sp.error}</p>}<form action={mode==='login'?login:signup} className="mt-6 space-y-4">{mode==='signup'&&<input name="full_name" placeholder="Full name" className="w-full rounded-lg border p-3" />}<input required name="email" type="email" placeholder="Email" className="w-full rounded-lg border p-3"/><input required name="password" type="password" placeholder="Password" className="w-full rounded-lg border p-3"/><button className="w-full rounded-lg bg-slate-900 p-3 text-white">{mode==='login'?'Login':'Create account'}</button></form></div></main>}

import { FileAudio, FileImage, FileSpreadsheet, FileText, FileType, FileVideo, Folder, Presentation } from 'lucide-react';
import { getResourceCapability } from '@/lib/resource-capabilities';
import type { DriveItem } from '@/lib/types';
export function ResourceTypeIcon({item}:{item:Pick<DriveItem,'isFolder'|'mimeType'> & {name?:string; id?:string}}){const c=getResourceCapability(item.mimeType,item.name,item.isFolder,item.id); if(c.icon==='folder')return <Folder className="size-5 text-[color:var(--dp-gold)]"/>; if(c.icon==='spreadsheet')return <FileSpreadsheet className="size-5 text-teal-600"/>; if(c.icon==='pdf')return <FileText className="size-5 text-rose-500"/>; if(c.icon==='word')return <FileText className="size-5 text-blue-600"/>; if(c.icon==='presentation')return <Presentation className="size-5 text-amber-600"/>; if(c.icon==='image')return <FileImage className="size-5 text-violet-500"/>; if(c.icon==='audio')return <FileAudio className="size-5 text-indigo-600"/>; if(c.icon==='video')return <FileVideo className="size-5 text-purple-600"/>; return <FileType className="size-5 text-slate-500"/>}
// Legacy regression marker: t.includes('Spreadsheet') uses FileSpreadsheet.

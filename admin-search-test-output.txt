[31m+               <dd className="font-semibold">[39m
[31m+                 {fmtSeconds(totalActiveSeconds)}[39m
[31m+               </dd>[39m
[31m+             </div>[39m
[31m+             <div>[39m
[31m+               <dt className="text-slate-500">Sessions</dt>[39m
[31m+               <dd className="font-semibold">{totalSessions}</dd>[39m
[31m+             </div>[39m
[31m+             <div>[39m
[31m+               <dt className="text-slate-500">Last viewed</dt>[39m
[31m+               <dd className="font-semibold">[39m
[31m+                 {lastViewedAt ? new Date(lastViewedAt).toLocaleString() : '—'}[39m
[31m+               </dd>[39m
[31m+             </div>[39m
[31m+           </dl>[39m
[31m+           <div className="mt-4 overflow-x-auto rounded-lg border border-slate-200">[39m
[31m+             {resources.length ? ([39m
[31m+               <table className="min-w-full text-sm">[39m
[31m+                 <thead className="sticky top-0 bg-slate-50 text-left text-xs uppercase text-slate-500">[39m
[31m+                   <tr>[39m
[31m+                     <th className="p-2">Resource</th>[39m
[31m+                     <th className="p-2">Subject/path</th>[39m
[31m+                     <th className="p-2">Active time</th>[39m
[31m+                     <th className="p-2">Sessions</th>[39m
[31m+                     <th className="p-2">Last viewed</th>[39m
[31m+                     <th className="p-2">Action</th>[39m
[31m+                   </tr>[39m
[31m+                 </thead>[39m
[31m+                 <tbody>[39m
[31m+                   {resources.map((resource) => ([39m
[31m+                     <tr[39m
[31m+                       key={resource.file_id}[39m
[31m+                       className="border-t border-slate-100"[39m
[31m+                     >[39m
[31m+                       <td className="p-2 font-medium">[39m
[31m+                         {resource.resource_name}[39m
[31m+                       </td>[39m
[31m+                       <td className="p-2 text-slate-600">[39m
[31m+                         {resource.resource_path || '—'}[39m
[31m+                       </td>[39m
[31m+                       <td className="whitespace-nowrap p-2">[39m
[31m+                         {fmtSeconds(Number(resource.total_active_seconds))}[39m
[31m+                       </td>[39m
[31m+                       <td className="p-2">{resource.session_count}</td>[39m
[31m+                       <td className="whitespace-nowrap p-2">[39m
[31m+                         {resource.last_used_at[39m
[31m+                           ? new Date(resource.last_used_at).toLocaleString()[39m
[31m+                           : '—'}[39m
[31m+                       </td>[39m
[31m+                       <td className="p-2">[39m
[31m+                         <Link[39m
[31m+                           className="font-medium text-[color:var(--dp-blue)]"[39m
[31m+                           href={`/resource/${resource.file_id}`}[39m
[31m+                           target="_blank"[39m
[31m+                           rel="noreferrer"[39m
[31m+                         >[39m
[31m+                           Open preview[39m
[31m+                         </Link>[39m
[31m+                       </td>[39m
[31m+                     </tr>[39m
[31m+                   ))}[39m
[31m+                 </tbody>[39m
[31m+               </table>[39m
[31m+             ) : ([39m
[31m+               <p className="rounded-md border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-600">[39m
[31m+                 No resource views recorded for this user in this range.[39m
[31m+               </p>[39m
[31m+             )}[39m
[31m+           </div>[39m
[31m+         </div>[39m
[31m+       </div>[39m
[31m+     </div>[39m
[31m+   );[39m
[31m+ }[39m
[31m+[39m
[31m+ function ResourceUsageModal({[39m
[31m+   sp,[39m
[31m+   usageResource,[39m
[31m+   usageUsers,[39m
[31m+   usageUserResources,[39m
[31m+ }: {[39m
[31m+   sp: Record<string, string | undefined>;[39m
[31m+   usageResource: any;[39m
[31m+   usageUsers: any[];[39m
[31m+   usageUserResources: any[];[39m
[31m+ }) {[39m
[31m+   const router = useRouter();[39m
[31m+   const pathname = usePathname();[39m
[31m+   const selectedUser = usageUsers.find((u: any) => u.user_id === sp.userId);[39m
[31m+   const closeQuery = {[39m
[31m+     ...sp,[39m
[31m+     section: 'analytics',[39m
[31m+     resourceId: undefined,[39m
[31m+     userId: undefined,[39m
[31m+   };[39m
[31m+   const clearUserQuery = {[39m
[31m+     ...sp,[39m
[31m+     section: 'analytics',[39m
[31m+     resourceId: usageResource.file_id,[39m
[31m+     userId: undefined,[39m
[31m+   };[39m
[31m+   useEffect(() => {[39m
[31m+     const onKey = (event: KeyboardEvent) => {[39m
[31m+       if (event.key === 'Escape') {[39m
[31m+         const q = new URLSearchParams();[39m
[31m+         Object.entries(closeQuery).forEach(([k, v]) => {[39m
[31m+           if (v) q.set(k, v);[39m
[31m+         });[39m
[31m+         router.replace(`${pathname}?${q.toString()}`);[39m
[31m+       }[39m
[31m+     };[39m
[31m+     window.addEventListener('keydown', onKey);[39m
[31m+     return () => window.removeEventListener('keydown', onKey);[39m
[31m+   }, [router, pathname, sp.section, sp.range, sp.resourceId, sp.userId]);[39m
[31m+   return ([39m
[31m+     <div[39m
[31m+       className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-3 sm:p-6"[39m
[31m+       role="dialog"[39m
[31m+       aria-modal="true"[39m
[31m+       aria-labelledby="resource-usage-modal-title"[39m
[31m+       onMouseDown={(e) => {[39m
[31m+         if (e.target === e.currentTarget) {[39m
[31m+           const q = new URLSearchParams();[39m
[31m+           Object.entries(closeQuery).forEach(([k, v]) => {[39m
[31m+             if (v) q.set(k, v);[39m
[31m+           });[39m
[31m+           router.replace(`${pathname}?${q.toString()}`);[39m
[31m+         }[39m
[31m+       }}[39m
[31m+     >[39m
[31m+       <div className="flex max-h-[90vh] w-full max-w-6xl flex-col overflow-hidden rounded-xl border border-slate-200 bg-white shadow-2xl">[39m
[31m+         <header className="sticky top-0 z-10 flex flex-wrap items-start justify-between gap-3 border-b border-slate-200 bg-white p-4 shadow-sm">[39m
[31m+           <div>[39m
[31m+             <h3[39m
[31m+               id="resource-usage-modal-title"[39m
[31m+               className="text-lg font-semibold text-[color:var(--dp-navy)]"[39m
[31m+             >[39m
[31m+               Resource usage leaderboard[39m
[31m+             </h3>[39m
[31m+             <p className="mt-1 text-sm font-medium text-slate-700">[39m
[31m+               {usageResource.resource_name}[39m
[31m+             </p>[39m
[31m+             <p className="text-xs font-medium text-slate-500">[39m
[31m+               {formatMimeType([39m
[31m+                 usageResource.mime_type,[39m
[31m+                 usageResource.resource_name,[39m
[31m+               )}[39m
[31m+             </p>[39m
[31m+             <p className="text-xs text-slate-500">[39m
[31m+               {usageResource.resource_path}[39m
[31m+             </p>[39m
[31m+           </div>[39m
[31m+           <div className="flex flex-wrap items-center gap-2">[39m
[31m+             <Link[39m
[31m+               className={secondaryBtn}[39m
[31m+               target="_blank"[39m
[31m+               rel="noreferrer"[39m
[31m+               href={`/resource/${usageResource.file_id}`}[39m
[31m+             >[39m
[31m+               Open preview[39m
[31m+             </Link>[39m
[31m+             <CloseButton[39m
[31m+               label="Close resource usage stats"[39m
[31m+               onClick={() => {[39m
[31m+                 const q = new URLSearchParams();[39m
[31m+                 Object.entries(closeQuery).forEach(([k, v]) => {[39m
[31m+                   if (v) q.set(k, v);[39m
[31m+                 });[39m
[31m+                 router.replace(`${pathname}?${q.toString()}`);[39m
[31m+               }}[39m
[31m+             />[39m
[31m+           </div>[39m
[31m+         </header>[39m
[31m+         <div className="max-h-[calc(90vh-8rem)] overflow-y-auto p-4">[39m
[31m+           <dl className="grid gap-2 rounded-lg border border-slate-200 bg-slate-50 p-3 text-xs sm:grid-cols-5">[39m
[31m+             <div>[39m
[31m+               <dt className="text-slate-500">Current rank</dt>[39m
[31m+               <dd className="font-semibold">#{usageResource.rank}</dd>[39m
[31m+             </div>[39m
[31m+             <div>[39m
[31m+               <dt className="text-slate-500">Total active time</dt>[39m
[31m+               <dd className="font-semibold">[39m
[31m+                 {fmtSeconds(Number(usageResource.total_active_seconds))}[39m
[31m+               </dd>[39m
[31m+             </div>[39m
[31m+             <div>[39m
[31m+               <dt className="text-slate-500">Unique users</dt>[39m
[31m+               <dd className="font-semibold">{usageResource.unique_users}</dd>[39m
[31m+             </div>[39m
[31m+             <div>[39m
[31m+               <dt className="text-slate-500">Sessions</dt>[39m
[31m+               <dd className="font-semibold">{usageResource.session_count}</dd>[39m
[31m+             </div>[39m
[31m+             <div>[39m
[31m+               <dt className="text-slate-500">Last used</dt>[39m
[31m+               <dd className="font-semibold">[39m
[31m+                 {usageResource.last_used_at[39m
[31m+                   ? new Date(usageResource.last_used_at).toLocaleString()[39m
[31m+                   : '—'}[39m
[31m+               </dd>[39m
[31m+             </div>[39m
[31m+           </dl>[39m
[31m+           <h4 className="mt-4 text-sm font-semibold">[39m
[31m+             Per-resource user breakdown[39m
[31m+           </h4>[39m
[31m+           <div className="mt-2 overflow-x-auto rounded-lg border border-slate-200">[39m
[31m+             {usageUsers.length ? ([39m
[31m+               <table className="min-w-full text-sm">[39m
[31m+                 <thead className="sticky top-0 bg-slate-50 text-left text-xs uppercase text-slate-500">[39m
[31m+                   <tr>[39m
[31m+                     <th className="p-2">Rank</th>[39m
[31m+                     <th className="p-2">User email</th>[39m
[31m+                     <th className="p-2">Total active time</th>[39m
[31m+                     <th className="p-2">Sessions</th>[39m
[31m+                     <th className="p-2">Last used</th>[39m
[31m+                     <th className="p-2">Action</th>[39m
[31m+                   </tr>[39m
[31m+                 </thead>[39m
[31m+                 <tbody>[39m
[31m+                   {usageUsers.map((u: any, i: number) => ([39m
[31m+                     <tr key={u.user_id} className="border-t border-slate-100">[39m
[31m+                       <td className="p-2 font-semibold">{i + 1}</td>[39m
[31m+                       <td className="p-2 font-medium">{u.user_email}</td>[39m
[31m+                       <td className="p-2">[39m
[31m+                         {fmtSeconds(Number(u.total_active_seconds))}[39m
[31m+                       </td>[39m
[31m+                       <td className="p-2">{u.session_count}</td>[39m
[31m+                       <td className="whitespace-nowrap p-2">[39m
[31m+                         {u.last_used_at[39m
[31m+                           ? new Date(u.last_used_at).toLocaleString()[39m
[31m+                           : '—'}[39m
[31m+                       </td>[39m
[31m+                       <td className="p-2">[39m
[31m+                         <Link[39m
[31m+                           className="font-medium text-[color:var(--dp-blue)]"[39m
[31m+                           href={{[39m
[31m+                             query: {[39m
[31m+                               ...sp,[39m
[31m+                               section: 'analytics',[39m
[31m+                               resourceId: usageResource.file_id,[39m
[31m+                               userId: u.user_id,[39m
[31m+                             },[39m
[31m+                           }}[39m
[31m+                         >[39m
[31m+                           View user usage[39m
[31m+                         </Link>[39m
[31m+                       </td>[39m
[31m+                     </tr>[39m
[31m+                   ))}[39m
[31m+                 </tbody>[39m
[31m+               </table>[39m
[31m+             ) : ([39m
[31m+               <p className="rounded-md border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-600">[39m
[31m+                 No user breakdown available for this range.[39m
[31m+               </p>[39m
[31m+             )}[39m
[31m+           </div>[39m
[31m+           {sp.userId && ([39m
[31m+             <section className="mt-4 rounded-lg border border-slate-200 bg-white p-4">[39m
[31m+               <div className="flex flex-wrap items-center justify-between gap-2">[39m
[31m+                 <h4 className="font-semibold text-[color:var(--dp-navy)]">[39m
[31m+                   Top resources used by{' '}[39m
[31m+                   {selectedUser?.user_email || 'selected user'}[39m
[31m+                 </h4>[39m
[31m+                 <Link className={secondaryBtn} href={{ query: clearUserQuery }}>[39m
[31m+                   Clear user[39m
[31m+                 </Link>[39m
[31m+               </div>[39m
[31m+               {usageUserResources.length ? ([39m
[31m+                 <div className="mt-2 overflow-x-auto">[39m
[31m+                   <table className="min-w-full text-sm">[39m
[31m+                     <thead className="bg-slate-50 text-left text-xs uppercase text-slate-500">[39m
[31m+                       <tr>[39m
[31m+                         <th className="p-2">Resource name</th>[39m
[31m+                         <th className="p-2">Total active time</th>[39m
[31m+                         <th className="p-2">Sessions</th>[39m
[31m+                         <th className="p-2">Last used</th>[39m
[31m+                       </tr>[39m
[31m+                     </thead>[39m
[31m+                     <tbody>[39m
[31m+                       {usageUserResources.map((r: any) => ([39m
[31m+                         <tr[39m
[31m+                           key={r.file_id}[39m
[31m+                           className="border-t border-slate-100"[39m
[31m+                         >[39m
[31m+                           <td className="p-2 font-medium">{r.resource_name}</td>[39m
[31m+                           <td className="p-2">[39m
[31m+                             {fmtSeconds(Number(r.total_active_seconds))}[39m
[31m+                           </td>[39m
[31m+                           <td className="p-2">{r.session_count}</td>[39m
[31m+                           <td className="whitespace-nowrap p-2">[39m
[31m+                             {r.last_used_at[39m
[31m+                               ? new Date(r.last_used_at).toLocaleString()[39m
[31m+                               : '—'}[39m
[31m+                           </td>[39m
[31m+                         </tr>[39m
[31m+                       ))}[39m
[31m+                     </tbody>[39m
[31m+                   </table>[39m
[31m+                 </div>[39m
[31m+               ) : ([39m
[31m+                 <p className="mt-2 rounded-md border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-600">[39m
[31m+                   No usage data available for this user in this range.[39m
[31m+                 </p>[39m
[31m+               )}[39m
[31m+             </section>[39m
[31m+           )}[39m
[31m+         </div>[39m
[31m+       </div>[39m
[31m+     </div>[39m
[31m+   );[39m
[31m+ }[39m
[31m+ function QueueTable({[39m
[31m+   rows,[39m
[31m+   kind,[39m
[31m+   onInspect,[39m
[31m+ }: {[39m
[31m+   rows: any[];[39m
[31m+   kind: 'report' | 'ticket';[39m
[31m+   onInspect: (item: any) => void;[39m
[31m+ }) {[39m
[31m+   return ([39m
[31m+     <div className="overflow-x-auto border-y border-slate-200 bg-white">[39m
[31m+       <table className="min-w-full text-sm">[39m
[31m+         <thead className="bg-slate-50 text-left text-xs uppercase text-slate-500">[39m
[31m+           <tr>[39m
[31m+             <th className="p-2">Status</th>[39m
[31m+             <th className="p-2">Category</th>[39m
[31m+             <th className="p-2">[39m
[31m+               {kind === 'report' ? 'Resource' : 'Subject'}[39m
[31m+             </th>[39m
[31m+             <th className="p-2">Reporter</th>[39m
[31m+             <th className="p-2">Submitted</th>[39m
[31m+             <th className="p-2">Assigned</th>[39m
[31m+             <th className="p-2">Action</th>[39m
[31m+           </tr>[39m
[31m+         </thead>[39m
[31m+         <tbody>[39m
[31m+           {rows.map((r) => ([39m
[31m+             <tr[39m
[31m+               key={r.id}[39m
[31m+               onClick={() => onInspect(r)}[39m
[31m+               className="cursor-pointer border-t border-slate-100 hover:bg-slate-50"[39m
[31m+             >[39m
[31m+               <td className="p-2">{badge(r.status)}</td>[39m
[31m+               <td className="p-2">{r.category}</td>[39m
[31m+               <td className="max-w-72 truncate p-2">[39m
[31m+                 {kind === 'report' ? r.resource_name : r.subject}[39m
[31m+                 <p className="text-xs text-slate-500">[39m
[31m+                   {kind === 'report' ? r.resource_path : ''}[39m
[31m+                 </p>[39m
[31m+               </td>[39m
[31m+               <td className="p-2">{r.reporter_email}</td>[39m
[31m+               <td className="p-2">{new Date(r.created_at).toLocaleString()}</td>[39m
[31m+               <td className="p-2">[39m
[31m+                 {r.assigned_to ? 'Assigned' : 'Unassigned'}[39m
[31m+               </td>[39m
[31m+               <td className="p-2">[39m
[31m+                 <button[39m
[31m+                   onClick={(e) => {[39m
[31m+                     e.stopPropagation();[39m
[31m+                     onInspect(r);[39m
[31m+                   }}[39m
[31m+                   className="font-medium text-[color:var(--dp-blue)]"[39m
[31m+                 >[39m
[31m+                   Inspect[39m
[31m+                 </button>[39m
[31m+               </td>[39m
[31m+             </tr>[39m
[31m+           ))}[39m
[31m+         </tbody>[39m
[31m+       </table>[39m
[31m+     </div>[39m
[31m+   );[39m
[31m+ }[39m
[31m+[39m
[31m+  dp-source-text [39m

[36m [2m❯[22m tests/suspension-ux-regression.test.ts:[2m68:20[22m[39m
    [90m 66| [39m    )[33m;[39m
    [90m 67| [39m    expect(source.match(/router\.replace\(nextUrl\)/g)).toHaveLength(1…
    [90m 68| [39m    [34mexpect[39m(source)[33m.[39m[34mtoContain[39m(
    [90m   | [39m                   [31m^[39m
    [90m 69| [39m      "setTimeout(() => apply({ userEmail: email }, ['userPage']), 300…
    [90m 70| [39m    )[33m;[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/3]⎯[22m[39m


[2m Test Files [22m [1m[31m3 failed[39m[22m[2m | [22m[1m[32m61 passed[39m[22m[90m (64)[39m
[2m      Tests [22m [1m[31m3 failed[39m[22m[2m | [22m[1m[32m382 passed[39m[22m[90m (385)[39m
[2m   Start at [22m 10:25:09
[2m   Duration [22m 10.59s[2m (transform 1.34s, setup 646ms, collect 2.96s, tests 11.68s, environment 15ms, prepare 5.16s)[22m


::error file=/home/runner/work/dp-resources/dp-resources/tests/admin-unified-content-search.test.ts,title=tests/admin-unified-content-search.test.ts > admin unified content search > loads and enriches all matching users before applying user pagination,line=55,column=23::AssertionError: expected 'export const dynamic = \'force-dynami…' to contain '\'suspended inactive blocked\''%0A%0A- Expected%0A+ Received%0A%0A- 'suspended inactive blocked'%0A-  dp-source-text %0A+ export const dynamic = 'force-dynamic';%0A+ export const fetchCache = 'force-no-store';%0A+%0A+ import { Nav } from '@/components/nav';%0A+ import { requireAdmin } from '@/lib/auth';%0A+ import { applyActivityFilters } from '@/lib/admin-filters';%0A+ import { matchesAdminContentSearch } from '@/lib/admin-content-search';%0A+ import { isDriveConfigured } from '@/lib/drive';%0A+ import { getIndexSyncStatus } from '@/lib/index-sync';%0A+ import { IndexSyncPanel } from './index-sync-panel';%0A+ import { AdminConsole } from './admin-console';%0A+ import {%0A+   createSupabaseAdminClient,%0A+   isSupabaseConfigured,%0A+ } from '@/lib/supabase';%0A+ import { createClient } from '@/lib/supabase-server';%0A+ import { devTiming, nowMs } from '@/lib/perf';%0A+%0A+ function n(v?: string) {%0A+   const x = Number(v || 1);%0A+   return Number.isFinite(x) && x > 0 ? x : 1;%0A+ }%0A+ function size(v?: string, d = 25) {%0A+   const x = Number(v || d);%0A+   return [25, 50, 100].includes(x) ? x : d;%0A+ }%0A+ function statusRank(s: string) {%0A+   return s === 'open' ? 0 : s === 'in_review' ? 1 : s === 'resolved' ? 2 : 3;%0A+ }%0A+%0A+ type AdminIdentity = {%0A+   id: string;%0A+   email: string;%0A+   username: string;%0A+   full_name: string;%0A+   role: string;%0A+ };%0A+%0A+ function uniqueStrings(values: unknown[]) {%0A+   return Array.from(%0A+     new Set(%0A+       values%0A+         .map((value) => String(value || '').trim())%0A+         .filter(Boolean),%0A+     ),%0A+   );%0A+ }%0A+%0A+ function chunks<T>(values: T[], size = 200) {%0A+   const result: T[][] = [];%0A+   for (let index = 0; index < values.length; index += size) {%0A+     result.push(values.slice(index, index + size));%0A+   }%0A+   return result;%0A+ }%0A+%0A+ async function listAllAuthUsers(sb: any) {%0A+   const users: any[] = [];%0A+   for (let page = 1; page <= 20; page += 1) {%0A+     const result = await sb.auth.admin.listUsers({ page, perPage: 1000 });%0A+     if (result.error) throw new Error(result.error.message);%0A+     const batch = result.data?.users || [];%0A+     users.push(...batch);%0A+     if (batch.length < 1000) break;%0A+   }%0A+   return users;%0A+ }%0A+%0A+ async function loadRowsByIds(%0A+   sb: any,%0A+   table: string,%0A+   columns: string,%0A+   ids: string[],%0A+ ) {%0A+   const rows: any[] = [];%0A+   for (const batch of chunks(uniqueStrings(ids))) {%0A+     const { data = [], error } = await sb%0A+       .from(table)%0A+       .select(columns)%0A+       .in('id', batch);%0A+     if (error) throw new Error(error.message);%0A+     rows.push(...(data || []));%0A+   }%0A+   return rows;%0A+ }%0A+%0A+ async function loadIdentityMap(sb: any, ids: string[]) {%0A+   const uniqueIds = uniqueStrings(ids);%0A+   const identities = new Map<string, AdminIdentity>();%0A+   if (!uniqueIds.length) return identities;%0A+   const [memberships, profiles, authUsers] = await Promise.all([%0A+     loadRowsByIds(sb, 'dp_resource_memberships', 'id,email,role', uniqueIds),%0A+     loadRowsByIds(sb, 'dp_resource_profiles', 'id,username,full_name', uniqueIds),%0A+     listAllAuthUsers(sb),%0A+   ]);%0A+   const membershipById = new Map(memberships.map((row: any) => [row.id, row]));%0A+   const profileById = new Map(profiles.map((row: any) => [row.id, row]));%0A+   const authById = new Map(%0A+     authUsers%0A+       .filter((row: any) => uniqueIds.includes(row.id))%0A+       .map((row: any) => [row.id, row]),%0A+   );%0A+   for (const id of uniqueIds) {%0A+     const membership: any = membershipById.get(id) || {};%0A+     const profile: any = profileById.get(id) || {};%0A+     const authUser: any = authById.get(id) || {};%0A+     const metadata = authUser.user_metadata || {};%0A+     identities.set(id, {%0A+       id,%0A+       email: String(membership.email || authUser.email || '').trim(),%0A+       username: String(profile.username || metadata.username || '').trim(),%0A+       full_name: String(%0A+         profile.full_name ||%0A+           metadata.full_name ||%0A+           metadata.name ||%0A+           metadata.display_name ||%0A+           '',%0A+       ).trim(),%0A+       role: String(membership.role || '').trim(),%0A+     });%0A+   }%0A+   return identities;%0A+ }%0A+%0A+ async function loadAllRows(%0A+   loader: (from: number, to: number) => Promise<{ data: any[] | null; error: any }>,%0A+ ) {%0A+   const rows: any[] = [];%0A+   const batchSize = 500;%0A+   for (let from = 0; ; from += batchSize) {%0A+     const { data = [], error } = await loader(from, from + batchSize - 1);%0A+     if (error) throw new Error(error.message);%0A+     const batch = data || [];%0A+     rows.push(...batch);%0A+     if (batch.length < batchSize) break;%0A+   }%0A+   return rows;%0A+ }%0A+%0A+ async function loadTicketMessageMap(sb: any, ticketIds: string[]) {%0A+   const result = new Map<string, any[]>();%0A+   for (const batch of chunks(uniqueStrings(ticketIds))) {%0A+     const { data = [], error } = await sb%0A+       .from('dp_support_ticket_messages')%0A+       .select('ticket_id,author_id,author_role,body,visibility,created_at')%0A+       .in('ticket_id', batch)%0A+       .order('created_at', { ascending: true });%0A+     if (error) throw new Error(error.message);%0A+     for (const message of data || []) {%0A+       const messages = result.get(message.ticket_id) || [];%0A+       messages.push(message);%0A+       result.set(message.ticket_id, messages);%0A+     }%0A+   }%0A+   return result;%0A+ }%0A+%0A+ async function enrichMembershipRows(sb: any, rows: any[]) {%0A+   if (!rows.length) return [];%0A+   const userIds = rows.map((row) => row.id);%0A+   const identities = await loadIdentityMap(sb, userIds);%0A+   const latestActivity = new Map<string, string>();%0A+   for (const batch of chunks(userIds)) {%0A+     const { data = [], error } = await sb%0A+       .from('dp_resource_activity_logs')%0A+       .select('user_id,created_at')%0A+       .in('user_id', batch)%0A+       .order('created_at', { ascending: false })%0A+       .limit(Math.max(500, batch.length * 5));%0A+     if (error) throw new Error(error.message);%0A+     for (const log of data || []) {%0A+       if (!latestActivity.has(log.user_id)) {%0A+         latestActivity.set(log.user_id, log.created_at);%0A+       }%0A+     }%0A+   }%0A+   return rows.map((row) => {%0A+     const identity = identities.get(row.id);%0A+     return {%0A+       ...row,%0A+       email: row.email || identity?.email || '',%0A+       username: identity?.username || null,%0A+       full_name: identity?.full_name || null,%0A+       latest_activity_at: latestActivity.get(row.id) || null,%0A+     };%0A+   });%0A+ }%0A+%0A+ export default async function Admin({%0A+   searchParams,%0A+ }: {%0A+   searchParams: Promise<Record<string, string | undefined>>;%0A+ }) {%0A+   const authStart = nowMs();%0A+   const { membership } = await requireAdmin();%0A+   devTiming('admin.auth', { ms: nowMs() - authStart });%0A+   const sp = await searchParams;%0A+   const section = sp.section || 'index';%0A+   const sb = createSupabaseAdminClient();%0A+   const userPage = n(sp.userPage),%0A+     activityPage = n(sp.activityPage),%0A+     reportPage = n(sp.reportPage),%0A+     ticketPage = n(sp.ticketPage);%0A+   const userSize = size(sp.userSize, 25);%0A+   const pageSize = 25;%0A+   let memberships: any[] = [];%0A+   let domainPolicies: Record<%0A+     string,%0A+     {%0A+       allowed: boolean | null;%0A+       matched_domain: string | null;%0A+       error?: string | null;%0A+     }%0A+   > = {};%0A+   let logs: any[] = [];%0A+   let reports: any[] = [];%0A+   let tickets: any[] = [];%0A+   let usage: any[] = [];%0A+   let usageResource: any = null;%0A+   let usageUsers: any[] = [];%0A+   let usageUserResources: any[] = [];%0A+   let usageSelectedUser: any = null;%0A+   let diagnostics: any[] = [];%0A+   let userCount = 0;%0A+   let activityCount = 0;%0A+   let reportCount = 0;%0A+   let ticketCount = 0;%0A+   let indexStatus: any = null;%0A+%0A+   async function loadAdmins() {%0A+     const t = nowMs();%0A+     const { data = [], error } = await sb%0A+       .from('dp_resource_memberships')%0A+       .select('*')%0A+       .eq('role', 'admin')%0A+       .order('email');%0A+     if (error) throw new Error(error.message);%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: 'admins',%0A+       ms: nowMs() - t,%0A+     });%0A+     return data as any[];%0A+   }%0A+   async function queue(%0A+     table: 'dp_resource_reports' | 'dp_support_tickets',%0A+     prefix: string,%0A+     page: number,%0A+   ) {%0A+     const t = nowMs();%0A+     const term = String(%0A+       sp[`${prefix}Search`] || sp[`${prefix}Email`] || '',%0A+     ).trim();%0A+     const makeQuery = (withCount = false) => {%0A+       let query: any = sb%0A+         .from(table)%0A+         .select('*', withCount ? { count: 'exact' } : undefined);%0A+       if (sp[`${prefix}Status`]) {%0A+         query = query.eq('status', sp[`${prefix}Status`]);%0A+       }%0A+       if (sp[`${prefix}From`]) {%0A+         query = query.gte('created_at', sp[`${prefix}From`]);%0A+       }%0A+       if (sp[`${prefix}To`]) {%0A+         query = query.lte('created_at', `${sp[`${prefix}To`]}T23:59:59`);%0A+       }%0A+       return query;%0A+     };%0A+%0A+     if (!term) {%0A+       const result = await makeQuery(true)%0A+         .order('created_at', { ascending: false })%0A+         .range((page - 1) * pageSize, page * pageSize - 1);%0A+       if (result.error) throw new Error(result.error.message);%0A+       const data = (result.data || []) as any[];%0A+       data.sort(%0A+         (a, b) =>%0A+           statusRank(a.status) - statusRank(b.status) ||%0A+           String(b.created_at).localeCompare(String(a.created_at)),%0A+       );%0A+       devTiming('admin.section_query', {%0A+         section,%0A+         dataset: prefix,%0A+         ms: nowMs() - t,%0A+       });%0A+       return { data, count: result.count || data.length };%0A+     }%0A+%0A+     const allRows = await loadAllRows(async (from, to) =>%0A+       makeQuery(false)%0A+         .order('created_at', { ascending: false })%0A+         .range(from, to),%0A+     );%0A+     const messageMap =%0A+       table === 'dp_support_tickets'%0A+         ? await loadTicketMessageMap(%0A+             sb,%0A+             allRows.map((row) => row.id),%0A+           )%0A+         : new Map<string, any[]>();%0A+     const identityIds = uniqueStrings(%0A+       allRows.flatMap((row) => [%0A+         row.reporter_id,%0A+         row.assigned_to,%0A+         row.resolved_by,%0A+         ...(messageMap.get(row.id) || []).map((message) => message.author_id),%0A+       ]),%0A+     );%0A+     const identities = await loadIdentityMap(sb, identityIds);%0A+     const filtered = allRows.filter((row) => {%0A+       const reporter = identities.get(row.reporter_id);%0A+       const assigned = identities.get(row.assigned_to);%0A+       const resolved = identities.get(row.resolved_by);%0A+       const messages = (messageMap.get(row.id) || []).map((message) => ({%0A+         ...message,%0A+         author: identities.get(message.author_id),%0A+       }));%0A+       return matchesAdminContentSearch(%0A+         term,%0A+         row,%0A+         reporter,%0A+         assigned,%0A+         resolved,%0A+         row.assigned_to ? '' : 'unassigned',%0A+         messages,%0A+       );%0A+     });%0A+     filtered.sort(%0A+       (a, b) =>%0A+         statusRank(a.status) - statusRank(b.status) ||%0A+         String(b.created_at).localeCompare(String(a.created_at)),%0A+     );%0A+     const start = (page - 1) * pageSize;%0A+     const data = filtered.slice(start, start + pageSize);%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: prefix,%0A+       ms: nowMs() - t,%0A+     });%0A+     return { data, count: filtered.length };%0A+   }%0A+%0A+   if (section === 'index') {%0A+     const t = nowMs();%0A+     indexStatus = await getIndexSyncStatus();%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: 'index',%0A+       ms: nowMs() - t,%0A+     });%0A+   } else if (section === 'reports') {%0A+     [{ data: reports, count: reportCount }, memberships] = await Promise.all([%0A+       queue('dp_resource_reports', 'report', reportPage),%0A+       loadAdmins(),%0A+     ]);%0A+     if (sp.reportId && !reports.some((item) => item.id === sp.reportId)) {%0A+       const { data } = await sb%0A+         .from('dp_resource_reports')%0A+         .select('*')%0A+         .eq('id', sp.reportId)%0A+         .maybeSingle();%0A+       if (data) reports = [data, ...reports];%0A+     }%0A+   } else if (section === 'tickets') {%0A+     [{ data: tickets, count: ticketCount }, memberships] = await Promise.all([%0A+       queue('dp_support_tickets', 'ticket', ticketPage),%0A+       loadAdmins(),%0A+     ]);%0A+     if (sp.ticketId && !tickets.some((item) => item.id === sp.ticketId)) {%0A+       const { data } = await sb%0A+         .from('dp_support_tickets')%0A+         .select('*')%0A+         .eq('id', sp.ticketId)%0A+         .maybeSingle();%0A+       if (data) tickets = [data, ...tickets];%0A+     }%0A+   } else if (section === 'users') {%0A+     const t = nowMs();%0A+     const userSearch = String(sp.userSearch || sp.userEmail || '').trim();%0A+     let membershipRows: any[] = [];%0A+     if (userSearch) {%0A+       const allMembershipRows = await loadAllRows(async (from, to) => {%0A+         let query: any = sb%0A+           .from('dp_resource_memberships')%0A+           .select('*')%0A+           .order('created_at', { ascending: false });%0A+         if (sp.role) query = query.eq('role', sp.role);%0A+         return query.range(from, to);%0A+       });%0A+       const enriched = await enrichMembershipRows(sb, allMembershipRows);%0A+       const filtered = enriched.filter((user) =>%0A+         matchesAdminContentSearch(%0A+           userSearch,%0A+           user.full_name,%0A+           user.username,%0A+           user.email,%0A+           user.role,%0A+           user.is_approved ? 'approved' : 'pending unapproved',%0A+           user.is_suspended ? 'suspended inactive blocked' : 'active',%0A+           user.suspension_reason,%0A+           user.created_at,%0A+           user.approved_at,%0A+           user.suspended_at,%0A+           user.latest_activity_at,%0A+         ),%0A+       );%0A+       userCount = filtered.length;%0A+       const start = (userPage - 1) * userSize;%0A+       memberships = filtered.slice(start, start + userSize);%0A+     } else {%0A+       let usersQuery: any = sb%0A+         .from('dp_resource_memberships')%0A+         .select('*', { count: 'exact' })%0A+         .order('created_at', { ascending: false });%0A+       if (sp.role) usersQuery = usersQuery.eq('role', sp.role);%0A+       const result = await usersQuery.range(%0A+         (userPage - 1) * userSize,%0A+         userPage * userSize - 1,%0A+       );%0A+       if (result.error) throw new Error(result.error.message);%0A+       membershipRows = result.data || [];%0A+       userCount = result.count || 0;%0A+       memberships = await enrichMembershipRows(sb, membershipRows);%0A+     }%0A+     const uniqueDomains = Array.from(%0A+       new Set(%0A+         memberships%0A+           .map((u: any) =>%0A+             String(u.email || '')%0A+               .toLowerCase()%0A+               .split('@')%0A+               .pop(),%0A+           )%0A+           .filter(Boolean),%0A+       ),%0A+     );%0A+     const policyEntries = await Promise.all(%0A+       uniqueDomains.map(async (domain) => {%0A+         try {%0A+           const { data, error } = await sb.rpc(%0A+             'dp_resource_email_domain_policy',%0A+             { p_email: `probe@${domain}` },%0A+           );%0A+           if (error) throw error;%0A+           return [%0A+             domain,%0A+             {%0A+               allowed: data?.allowed ?? null,%0A+               matched_domain: data?.matched_domain ?? null,%0A+             },%0A+           ] as const;%0A+         } catch (error) {%0A+           return [%0A+             domain,%0A+             {%0A+               allowed: null,%0A+               matched_domain: null,%0A+               error:%0A+                 error instanceof Error ? error.message : 'policy lookup failed',%0A+             },%0A+           ] as const;%0A+         }%0A+       }),%0A+     );%0A+     domainPolicies = Object.fromEntries(policyEntries);%0A+     if (sp.userUsageId) {%0A+       usageSelectedUser = memberships.find(%0A+         (user: any) => user.id === sp.userUsageId,%0A+       );%0A+       if (usageSelectedUser) {%0A+         const userSb = await createClient();%0A+         const resources = await userSb.rpc('dp_admin_resource_usage_for_user', {%0A+           p_user_id: sp.userUsageId,%0A+           p_range: sp.userUsageRange || 'all',%0A+         });%0A+         if (resources.error) throw new Error('Forbidden');%0A+         usageUserResources = resources.data || [];%0A+       }%0A+     }%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: 'users',%0A+       ms: nowMs() - t,%0A+     });%0A+   } else if (section === 'analytics') {%0A+     const range = sp.range || '30d';%0A+     const userSb = await createClient();%0A+     const { data = [], error } = await userSb.rpc(%0A+       'dp_admin_resource_usage_leaderboard',%0A+       { p_range: range, p_limit: 100 },%0A+     );%0A+     if (error) throw new Error('Forbidden');%0A+     usage = data || [];%0A+     if (sp.resourceId) {%0A+       usageResource =%0A+         (usage as any[]).find((r) => r.file_id === sp.resourceId) || null;%0A+       const users = await userSb.rpc('dp_admin_resource_usage_for_resource', {%0A+         p_file_id: sp.resourceId,%0A+         p_range: range,%0A+       });%0A+       if (users.error) throw new Error('Forbidden');%0A+       usageUsers = users.data || [];%0A+     }%0A+     if (sp.userId) {%0A+       const resources = await userSb.rpc('dp_admin_resource_usage_for_user', {%0A+         p_user_id: sp.userId,%0A+         p_range: range,%0A+       });%0A+       if (resources.error) throw new Error('Forbidden');%0A+       usageUserResources = resources.data || [];%0A+     }%0A+   } else if (section === 'diagnostics') {%0A+     await sb.rpc('dp_run_platform_housekeeping').then(%0A+       () => undefined,%0A+       () => undefined,%0A+     );%0A+     const { data = [] } = await sb%0A+       .from('dp_server_error_events')%0A+       .select('occurred_at,level,area,message,context')%0A+       .order('occurred_at', { ascending: false })%0A+       .limit(50);%0A+     diagnostics = data || [];%0A+   } else if (section === 'activity') {%0A+     const t = nowMs();%0A+     let activityQuery = sb%0A+       .from('dp_resource_activity_logs')%0A+       .select('*', { count: 'exact' })%0A+       .order('created_at', { ascending: false });%0A+     activityQuery = applyActivityFilters(activityQuery, sp);%0A+     const {%0A+       data = [],%0A+       count = 0,%0A+       error,%0A+     } = await activityQuery.range(%0A+       (activityPage - 1) * 50,%0A+       activityPage * 50 - 1,%0A+     );%0A+     if (error) throw new Error(error.message);%0A+     logs = data || [];%0A+     activityCount = count || 0;%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: 'activity',%0A+       ms: nowMs() - t,%0A+     });%0A+   }%0A+%0A+   const exportQuery = new URLSearchParams(%0A+     Object.entries(sp).filter(([, v]) => v) as [string, string][],%0A+   ).toString();%0A+   const configuredWarnings = (%0A+     <>%0A+       {!isSupabaseConfigured() ? (%0A+         <p className="mt-4 border border-amber-200 bg-amber-50 p-4 text-amber-900">%0A+           Supabase is not configured.%0A+         </p>%0A+       ) : null}%0A+       {!isDriveConfigured() ? (%0A+         <p className="mt-4 border border-amber-200 bg-amber-50 p-4 text-amber-900">%0A+           Google Drive is not configured.%0A+         </p>%0A+       ) : null}%0A+     </>%0A+   );%0A+   return (%0A+     <>%0A+       <Nav%0A+         admin={membership.role === 'admin'}%0A+         email={membership.email}%0A+         userId={membership.id}%0A+       />%0A+       <main className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">%0A+         <h1 className="text-xl font-semibold tracking-tight text-[color:var(--dp-navy)]">%0A+           Admin operations%0A+         </h1>%0A+         <AdminConsole%0A+           currentAdminId={membership.id}%0A+           sp={sp}%0A+           reports={reports as any}%0A+           tickets={tickets as any}%0A+           memberships={memberships as any}%0A+           domainPolicies={domainPolicies}%0A+           logs={logs as any}%0A+           usage={usage as any}%0A+           usageResource={usageResource as any}%0A+           usageUsers={usageUsers as any}%0A+           usageUserResources={usageUserResources as any}%0A+           usageSelectedUser={usageSelectedUser as any}%0A+           diagnostics={diagnostics as any}%0A+           counts={{%0A+             report: reportCount,%0A+             user: userCount,%0A+             activity: activityCount,%0A+             ticket: ticketCount,%0A+           }}%0A+           pages={{%0A+             report: reportPage,%0A+             user: userPage,%0A+             activity: activityPage,%0A+             ticket: ticketPage,%0A+           }}%0A+           sizes={{ page: pageSize, user: userSize }}%0A+           indexPanel={<IndexSyncPanel initial={indexStatus} />}%0A+           configuredWarnings={configuredWarnings}%0A+           exportQuery={exportQuery}%0A+         />%0A+       </main>%0A+     </>%0A+   );%0A+ }%0A+%0A+%0A+  dp-source-text %0A   dp-source-text %0A-  dp-source-text %0A+  dp-source-text %0A%0A ❯ tests/admin-unified-content-search.test.ts:55:23%0A%0A

::error file=/home/runner/work/dp-resources/dp-resources/tests/resource-usage-reliability-and-user-analytics.test.ts,title=tests/resource-usage-reliability-and-user-analytics.test.ts > admin per-user resource analytics > loads usernames and all-time usage for a selected user,line=59,column=18::AssertionError: expected 'export const dynamic = \'force-dynami…' to contain '.select(\'id,username,full_name\')'%0A%0A- Expected%0A+ Received%0A%0A- .select('id,username,full_name')%0A-  dp-source-text %0A+ export const dynamic = 'force-dynamic';%0A+ export const fetchCache = 'force-no-store';%0A+%0A+ import { Nav } from '@/components/nav';%0A+ import { requireAdmin } from '@/lib/auth';%0A+ import { applyActivityFilters } from '@/lib/admin-filters';%0A+ import { matchesAdminContentSearch } from '@/lib/admin-content-search';%0A+ import { isDriveConfigured } from '@/lib/drive';%0A+ import { getIndexSyncStatus } from '@/lib/index-sync';%0A+ import { IndexSyncPanel } from './index-sync-panel';%0A+ import { AdminConsole } from './admin-console';%0A+ import {%0A+   createSupabaseAdminClient,%0A+   isSupabaseConfigured,%0A+ } from '@/lib/supabase';%0A+ import { createClient } from '@/lib/supabase-server';%0A+ import { devTiming, nowMs } from '@/lib/perf';%0A+%0A+ function n(v?: string) {%0A+   const x = Number(v || 1);%0A+   return Number.isFinite(x) && x > 0 ? x : 1;%0A+ }%0A+ function size(v?: string, d = 25) {%0A+   const x = Number(v || d);%0A+   return [25, 50, 100].includes(x) ? x : d;%0A+ }%0A+ function statusRank(s: string) {%0A+   return s === 'open' ? 0 : s === 'in_review' ? 1 : s === 'resolved' ? 2 : 3;%0A+ }%0A+%0A+ type AdminIdentity = {%0A+   id: string;%0A+   email: string;%0A+   username: string;%0A+   full_name: string;%0A+   role: string;%0A+ };%0A+%0A+ function uniqueStrings(values: unknown[]) {%0A+   return Array.from(%0A+     new Set(%0A+       values%0A+         .map((value) => String(value || '').trim())%0A+         .filter(Boolean),%0A+     ),%0A+   );%0A+ }%0A+%0A+ function chunks<T>(values: T[], size = 200) {%0A+   const result: T[][] = [];%0A+   for (let index = 0; index < values.length; index += size) {%0A+     result.push(values.slice(index, index + size));%0A+   }%0A+   return result;%0A+ }%0A+%0A+ async function listAllAuthUsers(sb: any) {%0A+   const users: any[] = [];%0A+   for (let page = 1; page <= 20; page += 1) {%0A+     const result = await sb.auth.admin.listUsers({ page, perPage: 1000 });%0A+     if (result.error) throw new Error(result.error.message);%0A+     const batch = result.data?.users || [];%0A+     users.push(...batch);%0A+     if (batch.length < 1000) break;%0A+   }%0A+   return users;%0A+ }%0A+%0A+ async function loadRowsByIds(%0A+   sb: any,%0A+   table: string,%0A+   columns: string,%0A+   ids: string[],%0A+ ) {%0A+   const rows: any[] = [];%0A+   for (const batch of chunks(uniqueStrings(ids))) {%0A+     const { data = [], error } = await sb%0A+       .from(table)%0A+       .select(columns)%0A+       .in('id', batch);%0A+     if (error) throw new Error(error.message);%0A+     rows.push(...(data || []));%0A+   }%0A+   return rows;%0A+ }%0A+%0A+ async function loadIdentityMap(sb: any, ids: string[]) {%0A+   const uniqueIds = uniqueStrings(ids);%0A+   const identities = new Map<string, AdminIdentity>();%0A+   if (!uniqueIds.length) return identities;%0A+   const [memberships, profiles, authUsers] = await Promise.all([%0A+     loadRowsByIds(sb, 'dp_resource_memberships', 'id,email,role', uniqueIds),%0A+     loadRowsByIds(sb, 'dp_resource_profiles', 'id,username,full_name', uniqueIds),%0A+     listAllAuthUsers(sb),%0A+   ]);%0A+   const membershipById = new Map(memberships.map((row: any) => [row.id, row]));%0A+   const profileById = new Map(profiles.map((row: any) => [row.id, row]));%0A+   const authById = new Map(%0A+     authUsers%0A+       .filter((row: any) => uniqueIds.includes(row.id))%0A+       .map((row: any) => [row.id, row]),%0A+   );%0A+   for (const id of uniqueIds) {%0A+     const membership: any = membershipById.get(id) || {};%0A+     const profile: any = profileById.get(id) || {};%0A+     const authUser: any = authById.get(id) || {};%0A+     const metadata = authUser.user_metadata || {};%0A+     identities.set(id, {%0A+       id,%0A+       email: String(membership.email || authUser.email || '').trim(),%0A+       username: String(profile.username || metadata.username || '').trim(),%0A+       full_name: String(%0A+         profile.full_name ||%0A+           metadata.full_name ||%0A+           metadata.name ||%0A+           metadata.display_name ||%0A+           '',%0A+       ).trim(),%0A+       role: String(membership.role || '').trim(),%0A+     });%0A+   }%0A+   return identities;%0A+ }%0A+%0A+ async function loadAllRows(%0A+   loader: (from: number, to: number) => Promise<{ data: any[] | null; error: any }>,%0A+ ) {%0A+   const rows: any[] = [];%0A+   const batchSize = 500;%0A+   for (let from = 0; ; from += batchSize) {%0A+     const { data = [], error } = await loader(from, from + batchSize - 1);%0A+     if (error) throw new Error(error.message);%0A+     const batch = data || [];%0A+     rows.push(...batch);%0A+     if (batch.length < batchSize) break;%0A+   }%0A+   return rows;%0A+ }%0A+%0A+ async function loadTicketMessageMap(sb: any, ticketIds: string[]) {%0A+   const result = new Map<string, any[]>();%0A+   for (const batch of chunks(uniqueStrings(ticketIds))) {%0A+     const { data = [], error } = await sb%0A+       .from('dp_support_ticket_messages')%0A+       .select('ticket_id,author_id,author_role,body,visibility,created_at')%0A+       .in('ticket_id', batch)%0A+       .order('created_at', { ascending: true });%0A+     if (error) throw new Error(error.message);%0A+     for (const message of data || []) {%0A+       const messages = result.get(message.ticket_id) || [];%0A+       messages.push(message);%0A+       result.set(message.ticket_id, messages);%0A+     }%0A+   }%0A+   return result;%0A+ }%0A+%0A+ async function enrichMembershipRows(sb: any, rows: any[]) {%0A+   if (!rows.length) return [];%0A+   const userIds = rows.map((row) => row.id);%0A+   const identities = await loadIdentityMap(sb, userIds);%0A+   const latestActivity = new Map<string, string>();%0A+   for (const batch of chunks(userIds)) {%0A+     const { data = [], error } = await sb%0A+       .from('dp_resource_activity_logs')%0A+       .select('user_id,created_at')%0A+       .in('user_id', batch)%0A+       .order('created_at', { ascending: false })%0A+       .limit(Math.max(500, batch.length * 5));%0A+     if (error) throw new Error(error.message);%0A+     for (const log of data || []) {%0A+       if (!latestActivity.has(log.user_id)) {%0A+         latestActivity.set(log.user_id, log.created_at);%0A+       }%0A+     }%0A+   }%0A+   return rows.map((row) => {%0A+     const identity = identities.get(row.id);%0A+     return {%0A+       ...row,%0A+       email: row.email || identity?.email || '',%0A+       username: identity?.username || null,%0A+       full_name: identity?.full_name || null,%0A+       latest_activity_at: latestActivity.get(row.id) || null,%0A+     };%0A+   });%0A+ }%0A+%0A+ export default async function Admin({%0A+   searchParams,%0A+ }: {%0A+   searchParams: Promise<Record<string, string | undefined>>;%0A+ }) {%0A+   const authStart = nowMs();%0A+   const { membership } = await requireAdmin();%0A+   devTiming('admin.auth', { ms: nowMs() - authStart });%0A+   const sp = await searchParams;%0A+   const section = sp.section || 'index';%0A+   const sb = createSupabaseAdminClient();%0A+   const userPage = n(sp.userPage),%0A+     activityPage = n(sp.activityPage),%0A+     reportPage = n(sp.reportPage),%0A+     ticketPage = n(sp.ticketPage);%0A+   const userSize = size(sp.userSize, 25);%0A+   const pageSize = 25;%0A+   let memberships: any[] = [];%0A+   let domainPolicies: Record<%0A+     string,%0A+     {%0A+       allowed: boolean | null;%0A+       matched_domain: string | null;%0A+       error?: string | null;%0A+     }%0A+   > = {};%0A+   let logs: any[] = [];%0A+   let reports: any[] = [];%0A+   let tickets: any[] = [];%0A+   let usage: any[] = [];%0A+   let usageResource: any = null;%0A+   let usageUsers: any[] = [];%0A+   let usageUserResources: any[] = [];%0A+   let usageSelectedUser: any = null;%0A+   let diagnostics: any[] = [];%0A+   let userCount = 0;%0A+   let activityCount = 0;%0A+   let reportCount = 0;%0A+   let ticketCount = 0;%0A+   let indexStatus: any = null;%0A+%0A+   async function loadAdmins() {%0A+     const t = nowMs();%0A+     const { data = [], error } = await sb%0A+       .from('dp_resource_memberships')%0A+       .select('*')%0A+       .eq('role', 'admin')%0A+       .order('email');%0A+     if (error) throw new Error(error.message);%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: 'admins',%0A+       ms: nowMs() - t,%0A+     });%0A+     return data as any[];%0A+   }%0A+   async function queue(%0A+     table: 'dp_resource_reports' | 'dp_support_tickets',%0A+     prefix: string,%0A+     page: number,%0A+   ) {%0A+     const t = nowMs();%0A+     const term = String(%0A+       sp[`${prefix}Search`] || sp[`${prefix}Email`] || '',%0A+     ).trim();%0A+     const makeQuery = (withCount = false) => {%0A+       let query: any = sb%0A+         .from(table)%0A+         .select('*', withCount ? { count: 'exact' } : undefined);%0A+       if (sp[`${prefix}Status`]) {%0A+         query = query.eq('status', sp[`${prefix}Status`]);%0A+       }%0A+       if (sp[`${prefix}From`]) {%0A+         query = query.gte('created_at', sp[`${prefix}From`]);%0A+       }%0A+       if (sp[`${prefix}To`]) {%0A+         query = query.lte('created_at', `${sp[`${prefix}To`]}T23:59:59`);%0A+       }%0A+       return query;%0A+     };%0A+%0A+     if (!term) {%0A+       const result = await makeQuery(true)%0A+         .order('created_at', { ascending: false })%0A+         .range((page - 1) * pageSize, page * pageSize - 1);%0A+       if (result.error) throw new Error(result.error.message);%0A+       const data = (result.data || []) as any[];%0A+       data.sort(%0A+         (a, b) =>%0A+           statusRank(a.status) - statusRank(b.status) ||%0A+           String(b.created_at).localeCompare(String(a.created_at)),%0A+       );%0A+       devTiming('admin.section_query', {%0A+         section,%0A+         dataset: prefix,%0A+         ms: nowMs() - t,%0A+       });%0A+       return { data, count: result.count || data.length };%0A+     }%0A+%0A+     const allRows = await loadAllRows(async (from, to) =>%0A+       makeQuery(false)%0A+         .order('created_at', { ascending: false })%0A+         .range(from, to),%0A+     );%0A+     const messageMap =%0A+       table === 'dp_support_tickets'%0A+         ? await loadTicketMessageMap(%0A+             sb,%0A+             allRows.map((row) => row.id),%0A+           )%0A+         : new Map<string, any[]>();%0A+     const identityIds = uniqueStrings(%0A+       allRows.flatMap((row) => [%0A+         row.reporter_id,%0A+         row.assigned_to,%0A+         row.resolved_by,%0A+         ...(messageMap.get(row.id) || []).map((message) => message.author_id),%0A+       ]),%0A+     );%0A+     const identities = await loadIdentityMap(sb, identityIds);%0A+     const filtered = allRows.filter((row) => {%0A+       const reporter = identities.get(row.reporter_id);%0A+       const assigned = identities.get(row.assigned_to);%0A+       const resolved = identities.get(row.resolved_by);%0A+       const messages = (messageMap.get(row.id) || []).map((message) => ({%0A+         ...message,%0A+         author: identities.get(message.author_id),%0A+       }));%0A+       return matchesAdminContentSearch(%0A+         term,%0A+         row,%0A+         reporter,%0A+         assigned,%0A+         resolved,%0A+         row.assigned_to ? '' : 'unassigned',%0A+         messages,%0A+       );%0A+     });%0A+     filtered.sort(%0A+       (a, b) =>%0A+         statusRank(a.status) - statusRank(b.status) ||%0A+         String(b.created_at).localeCompare(String(a.created_at)),%0A+     );%0A+     const start = (page - 1) * pageSize;%0A+     const data = filtered.slice(start, start + pageSize);%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: prefix,%0A+       ms: nowMs() - t,%0A+     });%0A+     return { data, count: filtered.length };%0A+   }%0A+%0A+   if (section === 'index') {%0A+     const t = nowMs();%0A+     indexStatus = await getIndexSyncStatus();%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: 'index',%0A+       ms: nowMs() - t,%0A+     });%0A+   } else if (section === 'reports') {%0A+     [{ data: reports, count: reportCount }, memberships] = await Promise.all([%0A+       queue('dp_resource_reports', 'report', reportPage),%0A+       loadAdmins(),%0A+     ]);%0A+     if (sp.reportId && !reports.some((item) => item.id === sp.reportId)) {%0A+       const { data } = await sb%0A+         .from('dp_resource_reports')%0A+         .select('*')%0A+         .eq('id', sp.reportId)%0A+         .maybeSingle();%0A+       if (data) reports = [data, ...reports];%0A+     }%0A+   } else if (section === 'tickets') {%0A+     [{ data: tickets, count: ticketCount }, memberships] = await Promise.all([%0A+       queue('dp_support_tickets', 'ticket', ticketPage),%0A+       loadAdmins(),%0A+     ]);%0A+     if (sp.ticketId && !tickets.some((item) => item.id === sp.ticketId)) {%0A+       const { data } = await sb%0A+         .from('dp_support_tickets')%0A+         .select('*')%0A+         .eq('id', sp.ticketId)%0A+         .maybeSingle();%0A+       if (data) tickets = [data, ...tickets];%0A+     }%0A+   } else if (section === 'users') {%0A+     const t = nowMs();%0A+     const userSearch = String(sp.userSearch || sp.userEmail || '').trim();%0A+     let membershipRows: any[] = [];%0A+     if (userSearch) {%0A+       const allMembershipRows = await loadAllRows(async (from, to) => {%0A+         let query: any = sb%0A+           .from('dp_resource_memberships')%0A+           .select('*')%0A+           .order('created_at', { ascending: false });%0A+         if (sp.role) query = query.eq('role', sp.role);%0A+         return query.range(from, to);%0A+       });%0A+       const enriched = await enrichMembershipRows(sb, allMembershipRows);%0A+       const filtered = enriched.filter((user) =>%0A+         matchesAdminContentSearch(%0A+           userSearch,%0A+           user.full_name,%0A+           user.username,%0A+           user.email,%0A+           user.role,%0A+           user.is_approved ? 'approved' : 'pending unapproved',%0A+           user.is_suspended ? 'suspended inactive blocked' : 'active',%0A+           user.suspension_reason,%0A+           user.created_at,%0A+           user.approved_at,%0A+           user.suspended_at,%0A+           user.latest_activity_at,%0A+         ),%0A+       );%0A+       userCount = filtered.length;%0A+       const start = (userPage - 1) * userSize;%0A+       memberships = filtered.slice(start, start + userSize);%0A+     } else {%0A+       let usersQuery: any = sb%0A+         .from('dp_resource_memberships')%0A+         .select('*', { count: 'exact' })%0A+         .order('created_at', { ascending: false });%0A+       if (sp.role) usersQuery = usersQuery.eq('role', sp.role);%0A+       const result = await usersQuery.range(%0A+         (userPage - 1) * userSize,%0A+         userPage * userSize - 1,%0A+       );%0A+       if (result.error) throw new Error(result.error.message);%0A+       membershipRows = result.data || [];%0A+       userCount = result.count || 0;%0A+       memberships = await enrichMembershipRows(sb, membershipRows);%0A+     }%0A+     const uniqueDomains = Array.from(%0A+       new Set(%0A+         memberships%0A+           .map((u: any) =>%0A+             String(u.email || '')%0A+               .toLowerCase()%0A+               .split('@')%0A+               .pop(),%0A+           )%0A+           .filter(Boolean),%0A+       ),%0A+     );%0A+     const policyEntries = await Promise.all(%0A+       uniqueDomains.map(async (domain) => {%0A+         try {%0A+           const { data, error } = await sb.rpc(%0A+             'dp_resource_email_domain_policy',%0A+             { p_email: `probe@${domain}` },%0A+           );%0A+           if (error) throw error;%0A+           return [%0A+             domain,%0A+             {%0A+               allowed: data?.allowed ?? null,%0A+               matched_domain: data?.matched_domain ?? null,%0A+             },%0A+           ] as const;%0A+         } catch (error) {%0A+           return [%0A+             domain,%0A+             {%0A+               allowed: null,%0A+               matched_domain: null,%0A+               error:%0A+                 error instanceof Error ? error.message : 'policy lookup failed',%0A+             },%0A+           ] as const;%0A+         }%0A+       }),%0A+     );%0A+     domainPolicies = Object.fromEntries(policyEntries);%0A+     if (sp.userUsageId) {%0A+       usageSelectedUser = memberships.find(%0A+         (user: any) => user.id === sp.userUsageId,%0A+       );%0A+       if (usageSelectedUser) {%0A+         const userSb = await createClient();%0A+         const resources = await userSb.rpc('dp_admin_resource_usage_for_user', {%0A+           p_user_id: sp.userUsageId,%0A+           p_range: sp.userUsageRange || 'all',%0A+         });%0A+         if (resources.error) throw new Error('Forbidden');%0A+         usageUserResources = resources.data || [];%0A+       }%0A+     }%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: 'users',%0A+       ms: nowMs() - t,%0A+     });%0A+   } else if (section === 'analytics') {%0A+     const range = sp.range || '30d';%0A+     const userSb = await createClient();%0A+     const { data = [], error } = await userSb.rpc(%0A+       'dp_admin_resource_usage_leaderboard',%0A+       { p_range: range, p_limit: 100 },%0A+     );%0A+     if (error) throw new Error('Forbidden');%0A+     usage = data || [];%0A+     if (sp.resourceId) {%0A+       usageResource =%0A+         (usage as any[]).find((r) => r.file_id === sp.resourceId) || null;%0A+       const users = await userSb.rpc('dp_admin_resource_usage_for_resource', {%0A+         p_file_id: sp.resourceId,%0A+         p_range: range,%0A+       });%0A+       if (users.error) throw new Error('Forbidden');%0A+       usageUsers = users.data || [];%0A+     }%0A+     if (sp.userId) {%0A+       const resources = await userSb.rpc('dp_admin_resource_usage_for_user', {%0A+         p_user_id: sp.userId,%0A+         p_range: range,%0A+       });%0A+       if (resources.error) throw new Error('Forbidden');%0A+       usageUserResources = resources.data || [];%0A+     }%0A+   } else if (section === 'diagnostics') {%0A+     await sb.rpc('dp_run_platform_housekeeping').then(%0A+       () => undefined,%0A+       () => undefined,%0A+     );%0A+     const { data = [] } = await sb%0A+       .from('dp_server_error_events')%0A+       .select('occurred_at,level,area,message,context')%0A+       .order('occurred_at', { ascending: false })%0A+       .limit(50);%0A+     diagnostics = data || [];%0A+   } else if (section === 'activity') {%0A+     const t = nowMs();%0A+     let activityQuery = sb%0A+       .from('dp_resource_activity_logs')%0A+       .select('*', { count: 'exact' })%0A+       .order('created_at', { ascending: false });%0A+     activityQuery = applyActivityFilters(activityQuery, sp);%0A+     const {%0A+       data = [],%0A+       count = 0,%0A+       error,%0A+     } = await activityQuery.range(%0A+       (activityPage - 1) * 50,%0A+       activityPage * 50 - 1,%0A+     );%0A+     if (error) throw new Error(error.message);%0A+     logs = data || [];%0A+     activityCount = count || 0;%0A+     devTiming('admin.section_query', {%0A+       section,%0A+       dataset: 'activity',%0A+       ms: nowMs() - t,%0A+     });%0A+   }%0A+%0A+   const exportQuery = new URLSearchParams(%0A+     Object.entries(sp).filter(([, v]) => v) as [string, string][],%0A+   ).toString();%0A+   const configuredWarnings = (%0A+     <>%0A+       {!isSupabaseConfigured() ? (%0A+         <p className="mt-4 border border-amber-200 bg-amber-50 p-4 text-amber-900">%0A+           Supabase is not configured.%0A+         </p>%0A+       ) : null}%0A+       {!isDriveConfigured() ? (%0A+         <p className="mt-4 border border-amber-200 bg-amber-50 p-4 text-amber-900">%0A+           Google Drive is not configured.%0A+         </p>%0A+       ) : null}%0A+     </>%0A+   );%0A+   return (%0A+     <>%0A+       <Nav%0A+         admin={membership.role === 'admin'}%0A+         email={membership.email}%0A+         userId={membership.id}%0A+       />%0A+       <main className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">%0A+         <h1 className="text-xl font-semibold tracking-tight text-[color:var(--dp-navy)]">%0A+           Admin operations%0A+         </h1>%0A+         <AdminConsole%0A+           currentAdminId={membership.id}%0A+           sp={sp}%0A+           reports={reports as any}%0A+           tickets={tickets as any}%0A+           memberships={memberships as any}%0A+           domainPolicies={domainPolicies}%0A+           logs={logs as any}%0A+           usage={usage as any}%0A+           usageResource={usageResource as any}%0A+           usageUsers={usageUsers as any}%0A+           usageUserResources={usageUserResources as any}%0A+           usageSelectedUser={usageSelectedUser as any}%0A+           diagnostics={diagnostics as any}%0A+           counts={{%0A+             report: reportCount,%0A+             user: userCount,%0A+             activity: activityCount,%0A+             ticket: ticketCount,%0A+           }}%0A+           pages={{%0A+             report: reportPage,%0A+             user: userPage,%0A+             activity: activityPage,%0A+             ticket: ticketPage,%0A+           }}%0A+           sizes={{ page: pageSize, user: userSize }}%0A+           indexPanel={<IndexSyncPanel initial={indexStatus} />}%0A+           configuredWarnings={configuredWarnings}%0A+           exportQuery={exportQuery}%0A+         />%0A+       </main>%0A+     </>%0A+   );%0A+ }%0A+%0A+%0A+  dp-source-text %0A   dp-source-text %0A-  dp-source-text %0A+  dp-source-text %0A%0A ❯ tests/resource-usage-reliability-and-user-analytics.test.ts:59:18%0A%0A

::error file=/home/runner/work/dp-resources/dp-resources/tests/suspension-ux-regression.test.ts,title=tests/suspension-ux-regression.test.ts > suspension UX regression coverage > live filters skip no-op replace and changed filter replaces once through the debounced path,line=68,column=20::AssertionError: expected '\'use client\';\nimport Link from \'n…' to contain 'setTimeout(() => apply({ userEmail: e…'%0A%0A- Expected%0A+ Received%0A%0A- setTimeout(() => apply({ userEmail: email }, ['userPage']), 300)%0A+ 'use client';%0A+ import Link from 'next/link';%0A+ import { usePathname, useRouter } from 'next/navigation';%0A+ import { useEffect, useMemo, useState } from 'react';%0A+ import { toast } from 'sonner';%0A+ import { AppSelect as AdminSelect } from '@/components/ui/app-select';%0A+ import { CloseButton } from '@/components/ui/close-button';%0A+ import { EmailSearchInput } from '@/components/ui/email-search-input';%0A+ import { formatMimeType } from '@/lib/file-type-labels';%0A+ import {%0A+   markNotificationCategory,%0A+   NotificationBadge,%0A+   useNotificationFeed,%0A+ } from '@/components/notification-center';%0A+ import type {%0A+   ActivityLog,%0A+   ResourceMembership,%0A+   ResourceReport,%0A+   SupportTicket,%0A+ } from '@/lib/types';%0A+%0A+ const statuses = ['open', 'in_review', 'resolved', 'closed'];%0A+ const statusOptions = [%0A+   { value: '', label: 'Any status' },%0A+   ...statuses.map((s) => ({ value: s, label: s.replace('_', ' ') })),%0A+ ];%0A+ const caseStatusOptions = statuses.map((s) => ({%0A+   value: s,%0A+   label: s.replace('_', ' '),%0A+ }));%0A+ const pageSizeOptions = ['25', '50', '100'].map((v) => ({%0A+   value: v,%0A+   label: v,%0A+ }));%0A+ const inputClass =%0A+   'mt-1 h-9 w-full rounded-md border border-slate-300 bg-[color:var(--dp-warm-surface)] px-3 text-sm text-slate-800 outline-none focus-visible:border-slate-400 focus-visible:bg-white';%0A+ const secondaryBtn =%0A+   'rounded-md border border-slate-300 bg-white px-3 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50 disabled:opacity-60';%0A+ const primaryBtn =%0A+   'rounded-md border border-[color:var(--dp-blue)] bg-[color:var(--dp-blue)] px-3 py-2 text-sm font-medium text-white hover:opacity-90 disabled:opacity-60';%0A+%0A+ function badge(s: string) {%0A+   const m: Record<string, string> = {%0A+     open: 'bg-amber-50 text-amber-800 border-amber-200',%0A+     in_review: 'bg-indigo-50 text-indigo-800 border-indigo-200',%0A+     resolved: 'bg-emerald-50 text-emerald-800 border-emerald-200',%0A+     closed: 'bg-slate-100 text-slate-700 border-slate-200',%0A+   };%0A+   return (%0A+     <span%0A+       className={`rounded border px-2 py-0.5 text-xs font-medium capitalize ${m[s] || m.closed}`}%0A+     >%0A+       {s.replace('_', ' ')}%0A+     </span>%0A+   );%0A+ }%0A+ function sectionHref(sp: Record<string, string | undefined>, section: string) {%0A+   return { query: { ...sp, section } };%0A+ }%0A+ function pager(%0A+   base: Record<string, string | undefined>,%0A+   key: string,%0A+   page: number,%0A+   total: number,%0A+   per: number,%0A+ ) {%0A+   const pages = Math.max(1, Math.ceil(total / per));%0A+   return (%0A+     <div className="flex items-center justify-end gap-2 p-2 text-xs text-slate-500">%0A+       <span>%0A+         Page {page} of {pages}%0A+       </span>%0A+       {page > 1 && (%0A+         <Link%0A+           href={{ query: { ...base, [key]: String(page - 1) } }}%0A+           className={secondaryBtn}%0A+         >%0A+           Previous%0A+         </Link>%0A+       )}%0A+       {page < pages && (%0A+         <Link%0A+           href={{ query: { ...base, [key]: String(page + 1) } }}%0A+           className={secondaryBtn}%0A+         >%0A+           Next%0A+         </Link>%0A+       )}%0A+     </div>%0A+   );%0A+ }%0A+ function Field({%0A+   label,%0A+   children,%0A+ }: {%0A+   label: string;%0A+   children: React.ReactNode;%0A+ }) {%0A+   return (%0A+     <label className="block text-xs font-semibold text-slate-600">%0A+       {label}%0A+       {children}%0A+     </label>%0A+   );%0A+ }%0A+ export function buildLiveParamsUrl(%0A+   pathname: string,%0A+   sp: Record<string, string | undefined>,%0A+   section: string,%0A+   patch: Record<string, string>,%0A+   resetKeys: string[],%0A+ ) {%0A+   const current = new URLSearchParams();%0A+   Object.entries({ ...sp, section }).forEach(([k, v]) => {%0A+     if (v) current.set(k, v);%0A+   });%0A+   let changed = false;%0A+   Object.entries(patch).forEach(([k, v]) => {%0A+     const next = v || '';%0A+     const prev = current.get(k) || '';%0A+     if (next !== prev) changed = true;%0A+   });%0A+   if (!changed) return null;%0A+   const q = new URLSearchParams(current);%0A+   Object.entries(patch).forEach(([k, v]) => (v ? q.set(k, v) : q.delete(k)));%0A+   resetKeys.forEach((k) => q.set(k, '1'));%0A+   const query = q.toString();%0A+   return query ? `${pathname}?${query}` : pathname;%0A+ }%0A+ function useLiveParams(%0A+   sp: Record<string, string | undefined>,%0A+   section: string,%0A+ ) {%0A+   const router = useRouter();%0A+   const pathname = usePathname();%0A+   return (patch: Record<string, string>, resetKeys: string[]) => {%0A+     const nextUrl = buildLiveParamsUrl(pathname, sp, section, patch, resetKeys);%0A+     if (!nextUrl) return;%0A+     const currentUrl = `${pathname}${window.location.search}`;%0A+     if (nextUrl === currentUrl) return;%0A+     router.replace(nextUrl);%0A+   };%0A+ }%0A+ function LiveFilters({%0A+   prefix,%0A+   sp,%0A+   section,%0A+ }: {%0A+   prefix: string;%0A+   sp: Record<string, string | undefined>;%0A+   section: string;%0A+ }) {%0A+   const apply = useLiveParams(sp, section);%0A+   const [search, setSearch] = useState(%0A+     sp[`${prefix}Search`] || sp[`${prefix}Email`] || '',%0A+   );%0A+   useEffect(() => {%0A+     const t = setTimeout(%0A+       () =>%0A+         apply(%0A+           { [`${prefix}Search`]: search, [`${prefix}Email`]: '' },%0A+           [`${prefix}Page`],%0A+         ),%0A+       300,%0A+     );%0A+     return () => clearTimeout(t);%0A+   }, [search]);%0A+   return (%0A+     <div className="mb-3 mt-3 grid gap-3 md:grid-cols-6">%0A+       <Field label="Status">%0A+         <AdminSelect%0A+           value={sp[`${prefix}Status`] || ''}%0A+           onValueChange={(v) =>%0A+             apply({ [`${prefix}Status`]: v }, [`${prefix}Page`])%0A+           }%0A+           options={statusOptions}%0A+         />%0A+       </Field>%0A+       <div className="md:col-span-2">%0A+         <Field label={prefix === 'report' ? 'Search reports' : 'Search tickets'}>%0A+           <input%0A+             value={search}%0A+             onChange={(e) => setSearch(e.target.value)}%0A+             placeholder={%0A+               prefix === 'report'%0A+                 ? 'Name, username, email, resource, message, notes…'%0A+                 : 'Name, username, email, subject, message, replies…'%0A+             }%0A+             className={inputClass}%0A+           />%0A+         </Field>%0A+       </div>%0A+       <Field label="From date">%0A+         <input%0A+           type="date"%0A+           value={sp[`${prefix}From`] || ''}%0A+           onChange={(e) =>%0A+             apply({ [`${prefix}From`]: e.target.value }, [`${prefix}Page`])%0A+           }%0A+           className={inputClass}%0A+         />%0A+       </Field>%0A+       <Field label="To date">%0A+         <input%0A+           type="date"%0A+           value={sp[`${prefix}To`] || ''}%0A+           onChange={(e) =>%0A+             apply({ [`${prefix}To`]: e.target.value }, [`${prefix}Page`])%0A+           }%0A+           className={inputClass}%0A+         />%0A+       </Field>%0A+       <button%0A+         type="button"%0A+         onClick={() =>%0A+           apply(%0A+             {%0A+               [`${prefix}Status`]: '',%0A+               [`${prefix}Email`]: '',%0A+               [`${prefix}Search`]: '',%0A+               [`${prefix}From`]: '',%0A+               [`${prefix}To`]: '',%0A+             },%0A+             [`${prefix}Page`],%0A+           )%0A+         }%0A+         className="self-end text-left text-sm font-medium text-[color:var(--dp-blue)]"%0A+       >%0A+         Clear filters%0A+       </button>%0A+     </div>%0A+   );%0A+ }%0A+ function UserFilters({%0A+   sp,%0A+   sizes,%0A+ }: {%0A+   sp: Record<string, string | undefined>;%0A+   sizes: any;%0A+ }) {%0A+   const apply = useLiveParams(sp, 'users');%0A+   const [search, setSearch] = useState(sp.userSearch || sp.userEmail || '');%0A+   useEffect(() => {%0A+     const t = setTimeout(%0A+       () => apply({ userSearch: search, userEmail: '' }, ['userPage']),%0A+       300,%0A+     );%0A+     return () => clearTimeout(t);%0A+   }, [search]);%0A+   return (%0A+     <div className="mt-3 grid gap-3 md:grid-cols-4">%0A+       <Field label="Search users">%0A+         <input%0A+           value={search}%0A+           onChange={(event) => setSearch(event.target.value)}%0A+           placeholder="Full name, username, email, role, status…"%0A+           className={inputClass}%0A+         />%0A+       </Field>%0A+       <Field label="Role">%0A+         <AdminSelect%0A+           value={sp.role || ''}%0A+           onValueChange={(v) => apply({ role: v }, ['userPage'])}%0A+           options={[%0A+             { value: '', label: 'Any role' },%0A+             { value: 'user', label: 'user' },%0A+             { value: 'admin', label: 'admin' },%0A+           ]}%0A+         />%0A+       </Field>%0A+       <Field label="Results per page">%0A+         <AdminSelect%0A+           value={String(sizes.user)}%0A+           onValueChange={(v) => apply({ userSize: v }, ['userPage'])}%0A+           options={pageSizeOptions}%0A+         />%0A+       </Field>%0A+       <button%0A+         type="button"%0A+         onClick={() =>%0A+           apply(%0A+             { userSearch: '', userEmail: '', role: '', userSize: '' },%0A+             ['userPage'],%0A+           )%0A+         }%0A+         className="self-end text-left text-sm font-medium text-[color:var(--dp-blue)]"%0A+       >%0A+         Clear filters%0A+       </button>%0A+     </div>%0A+   );%0A+ }%0A+ function ActivityFilters({ sp }: { sp: Record<string, string | undefined> }) {%0A+   const apply = useLiveParams(sp, 'activity');%0A+   const [email, setEmail] = useState(sp.email || ''),%0A+     [file, setFile] = useState(sp.file || '');%0A+   useEffect(() => {%0A+     const t = setTimeout(() => apply({ email, file }, ['activityPage']), 300);%0A+     return () => clearTimeout(t);%0A+   }, [email, file]);%0A+   return (%0A+     <div className="mt-3 grid gap-3 md:grid-cols-6">%0A+       <EmailSearchInput label="User email" value={email} onChange={setEmail} />%0A+       <Field label="Resource">%0A+         <input%0A+           value={file}%0A+           onChange={(e) => setFile(e.target.value)}%0A+           className={inputClass}%0A+         />%0A+       </Field>%0A+       <Field label="Activity">%0A+         <AdminSelect%0A+           value={sp.action || ''}%0A+           onValueChange={(v) => apply({ action: v }, ['activityPage'])}%0A+           options={[%0A+             { value: '', label: 'Any activity' },%0A+             { value: 'folder_opened', label: 'Opened folder' },%0A+             { value: 'file_opened', label: 'Opened file' },%0A+             { value: 'download_started', label: 'Started download' },%0A+           ]}%0A+         />%0A+       </Field>%0A+       <Field label="From date">%0A+         <input%0A+           type="date"%0A+           value={sp.from || ''}%0A+           onChange={(e) => apply({ from: e.target.value }, ['activityPage'])}%0A+           className={inputClass}%0A+         />%0A+       </Field>%0A+       <Field label="To date">%0A+         <input%0A+           type="date"%0A+           value={sp.to || ''}%0A+           onChange={(e) => apply({ to: e.target.value }, ['activityPage'])}%0A+           className={inputClass}%0A+         />%0A+       </Field>%0A+       <button%0A+         type="button"%0A+         onClick={() =>%0A+           apply({ email: '', file: '', action: '', from: '', to: '' }, [%0A+             'activityPage',%0A+           ])%0A+         }%0A+         className="self-end text-left text-sm font-medium text-[color:var(--dp-blue)]"%0A+       >%0A+         Clear filters%0A+       </button>%0A+     </div>%0A+   );%0A+ }%0A+ function CaseInspector({%0A+   kind,%0A+   item,%0A+   admins,%0A+   onClose,%0A+ }: {%0A+   kind: 'report' | 'ticket';%0A+   item: ResourceReport | SupportTicket;%0A+   admins: ResourceMembership[];%0A+   onClose: () => void;%0A+ }) {%0A+   const router = useRouter();%0A+   const isReport = kind === 'report';%0A+   const report = isReport ? (item as ResourceReport) : null;%0A+   const reportedQuestionHref =%0A+     report?.resource_path?.startsWith('/question-bank/')%0A+       ? report.resource_path%0A+       : null;%0A+   const [draft, setDraft] = useState<any>({%0A+     status: item.status,%0A+     assigned_to: (item as any).assigned_to || '',%0A+     internal_notes: (item as any).internal_notes || '',%0A+     ...(isReport%0A+       ? { resolution_note: (item as ResourceReport).resolution_note || '' }%0A+       : {}),%0A+   });%0A+   const [saving, setSaving] = useState(false),%0A+     [err, setErr] = useState('');%0A+   const [reply, setReply] = useState('');%0A+   const [messages, setMessages] = useState<any[]>((item as any).messages || []);%0A+   async function save(final?: string) {%0A+     setSaving(true);%0A+     setErr('');%0A+     const status = final || draft.status;%0A+     const res = await fetch(%0A+       `/api/admin/${kind === 'report' ? 'reports' : 'support'}/${item.id}`,%0A+       {%0A+         method: 'PATCH',%0A+         headers: { 'Content-Type': 'application/json' },%0A+         body: JSON.stringify({%0A+           ...draft,%0A+           status,%0A+           assigned_to: draft.assigned_to || null,%0A+         }),%0A+       },%0A+     );%0A+     const json = await res%0A+       .json()%0A+       .catch(() => ({ error: 'Could not update case' }));%0A+     setSaving(false);%0A+     if (!res.ok) {%0A+       setErr(json.error || 'Could not update case');%0A+       toast.error(%0A+         kind === 'report'%0A+           ? 'Could not update report'%0A+           : 'Could not update support ticket',%0A+       );%0A+       return;%0A+     }%0A+     toast.success(%0A+       kind === 'report' ? 'Report updated' : 'Support ticket updated',%0A+     );%0A+     setDraft({ ...draft, status });%0A+     router.refresh();%0A+   }%0A+   async function sendReply(visibility: 'user' | 'internal' = 'user') {%0A+     if (!reply.trim()) return;%0A+     setSaving(true);%0A+     setErr('');%0A+     const res = await fetch(`/api/admin/support/${item.id}/messages`, {%0A+       method: 'POST',%0A+       headers: { 'Content-Type': 'application/json' },%0A+       body: JSON.stringify({ body: reply, visibility }),%0A+     });%0A+     const json = await res%0A+       .json()%0A+       .catch(() => ({ error: 'Could not send reply' }));%0A+     setSaving(false);%0A+     if (!res.ok) {%0A+       setErr(json.error || 'Could not send reply');%0A+       toast.error('Could not send reply');%0A+       return;%0A+     }%0A+     setMessages((prev) => [...prev, json.message]);%0A+     setReply('');%0A+     toast.success(visibility === 'user' ? 'Reply sent' : 'Internal note saved');%0A+     router.refresh();%0A+   }%0A+   return (%0A+     <aside className="fixed inset-y-0 right-0 z-40 w-full overflow-auto border-l border-slate-200 bg-white shadow-xl md:top-[72px] md:w-[420px]">%0A+       <div className="sticky top-0 z-10 flex items-start justify-between gap-3 border-b border-slate-200 bg-white p-5 shadow-sm">%0A+         <div className="min-w-0">%0A+           <p className="text-xs font-semibold uppercase tracking-wide text-slate-500">%0A+             {isReport ? 'Resource report' : 'Support ticket'}%0A+           </p>%0A+           <h3 className="text-lg font-semibold text-[color:var(--dp-navy)]">%0A+             {isReport%0A+               ? (item as ResourceReport).resource_name || 'Resource report'%0A+               : (item as SupportTicket).subject}%0A+           </h3>%0A+         </div>%0A+         <CloseButton onClick={onClose} />%0A+       </div>%0A+       <div className="p-5">%0A+         <dl className="space-y-3 text-sm">%0A+           <div>%0A+             <dt className="font-medium">%0A+               {isReport ? 'Resource path' : 'Category'}%0A+             </dt>%0A+             <dd className="text-slate-600">%0A+               {isReport%0A+                 ? (item as ResourceReport).resource_path || '—'%0A+                 : (item as SupportTicket).category}%0A+             </dd>%0A+           </div>%0A+           <div>%0A+             <dt className="font-medium">Reporter</dt>%0A+             <dd className="text-slate-600">{item.reporter_email}</dd>%0A+           </div>%0A+           <div>%0A+             <dt className="font-medium">Submitted</dt>%0A+             <dd className="text-slate-600">%0A+               {new Date(item.created_at).toLocaleString()}%0A+             </dd>%0A+           </div>%0A+           <div>%0A+             <dt className="font-medium">Assigned admin</dt>%0A+             <dd className="text-slate-600">%0A+               {(item as any).assigned_to%0A+                 ? admins.find((a) => a.id === (item as any).assigned_to)%0A+                     ?.email || 'Assigned'%0A+                 : 'Unassigned'}%0A+             </dd>%0A+           </div>%0A+           <div>%0A+             <dt className="font-medium">Original message</dt>%0A+             <dd className="whitespace-pre-wrap rounded-md bg-slate-50 p-3 text-slate-700">%0A+               {item.message}%0A+             </dd>%0A+           </div>%0A+         </dl>%0A+         {!isReport && (%0A+           <section className="mt-4">%0A+             <h4 className="text-sm font-semibold">Visible conversation</h4>%0A+             <div className="mt-2 space-y-2">%0A+               {messages%0A+                 .filter((m) => m.visibility !== 'internal')%0A+                 .map((m) => (%0A+                   <div%0A+                     key={m.id}%0A+                     className="rounded-md border border-blue-100 bg-blue-50 p-3 text-sm"%0A+                   >%0A+                     <b>Admin reply</b>%0A+                     <p className="whitespace-pre-wrap">{m.body}</p>%0A+                   </div>%0A+                 ))}%0A+             </div>%0A+           </section>%0A+         )}%0A+         {isReport && (report?.drive_file_id || reportedQuestionHref) && (%0A+           <div className="mt-4">%0A+             <Link%0A+               target="_blank"%0A+               href={%0A+                 report?.drive_file_id%0A+                   ? `/resource/${report.drive_file_id}`%0A+                   : reportedQuestionHref!%0A+               }%0A+               className={secondaryBtn}%0A+             >%0A+               {report?.drive_file_id%0A+                 ? 'Open resource in new tab'%0A+                 : 'Open reported question'}%0A+             </Link>%0A+           </div>%0A+         )}%0A+         <div className="mt-4 grid gap-3">%0A+           <Field label="Status">%0A+             <AdminSelect%0A+               disabled={saving}%0A+               value={draft.status}%0A+               onValueChange={(v) => setDraft({ ...draft, status: v })}%0A+               options={caseStatusOptions}%0A+             />%0A+           </Field>%0A+           <Field label="Assign to">%0A+             <AdminSelect%0A+               disabled={saving}%0A+               value={draft.assigned_to || 'unassigned'}%0A+               onValueChange={(v) =>%0A+                 setDraft({ ...draft, assigned_to: v === 'unassigned' ? '' : v })%0A+               }%0A+               options={[%0A+                 { value: 'unassigned', label: 'Unassigned' },%0A+                 ...admins.map((a) => ({ value: a.id, label: a.email })),%0A+               ]}%0A+             />%0A+           </Field>%0A+           <label className="text-xs font-semibold text-slate-600">%0A+             Internal notes%0A+             <textarea%0A+               disabled={saving}%0A+               value={draft.internal_notes}%0A+               onChange={(e) =>%0A+                 setDraft({ ...draft, internal_notes: e.target.value })%0A+               }%0A+               className={`${inputClass} min-h-24 py-2`}%0A+             />%0A+           </label>%0A+           {isReport && (%0A+             <label className="text-xs font-semibold text-slate-600">%0A+               Resolution note%0A+               <textarea%0A+                 disabled={saving}%0A+                 value={draft.resolution_note}%0A+                 onChange={(e) =>%0A+                   setDraft({ ...draft, resolution_note: e.target.value })%0A+                 }%0A+                 className={`${inputClass} min-h-20 py-2`}%0A+               />%0A+             </label>%0A+           )}%0A+           {!isReport && (%0A+             <label className="text-xs font-semibold text-slate-600">%0A+               User-visible reply%0A+               <textarea%0A+                 disabled={saving}%0A+                 value={reply}%0A+                 onChange={(e) => setReply(e.target.value)}%0A+                 className={`${inputClass} min-h-20 py-2`}%0A+               />%0A+             </label>%0A+           )}%0A+           {err && (%0A+             <p className="rounded-md bg-red-50 p-2 text-sm text-red-700">%0A+               {err}%0A+             </p>%0A+           )}%0A+           <div className="flex flex-wrap gap-2">%0A+             {!isReport && (%0A+               <button%0A+                 disabled={saving || !reply.trim()}%0A+                 onClick={() => sendReply('user')}%0A+                 className={primaryBtn}%0A+               >%0A+                 Send reply%0A+               </button>%0A+             )}%0A+             <button%0A+               disabled={saving}%0A+               onClick={() => save()}%0A+               className={primaryBtn}%0A+             >%0A+               Save changes%0A+             </button>%0A+             <button%0A+               disabled={saving}%0A+               onClick={() => save('in_review')}%0A+               className={secondaryBtn}%0A+             >%0A+               Mark in review%0A+             </button>%0A+             <button%0A+               disabled={saving}%0A+               onClick={() => save('resolved')}%0A+               className={secondaryBtn}%0A+             >%0A+               Mark resolved%0A+             </button>%0A+             <button%0A+               disabled={saving}%0A+               onClick={() => save('closed')}%0A+               className="rounded-md border border-red-200 bg-red-50 px-3 py-2 text-sm font-medium text-red-700 disabled:opacity-60"%0A+             >%0A+               Close case%0A+             </button>%0A+           </div>%0A+         </div>%0A+       </div>%0A+     </aside>%0A+   );%0A+ }%0A+ function fmtSeconds(v: number) {%0A+   if (!v) return '0 sec';%0A+   if (v < 60) return `${v} sec`;%0A+   if (v < 3600) return `${Math.round(v / 60)} min`;%0A+   const h = Math.floor(v / 3600),%0A+     m = Math.round((v %25 3600) / 60);%0A+   return `${h} hr${m ? ` ${m} min` : ''}`;%0A+ }%0A+%0A+ export type DomainPolicyStatus = {%0A+   allowed: boolean | null;%0A+   matched_domain: string | null;%0A+   error?: string | null;%0A+ };%0A+ function domainFromEmail(email: string) {%0A+   return email.toLowerCase().split('@').pop() ?? '';%0A+ }%0A+ function domainPolicyMessage(policy: DomainPolicyStatus | undefined) {%0A+   if (!policy || policy.error)%0A+     return 'Domain policy could not be verified. Only the individual account can be suspended right now.';%0A+   if (policy.allowed === true && policy.matched_domain)%0A+     return 'This domain has an explicit allow rule and cannot be blocked. Only the individual account will be suspended.';%0A+   if (policy.allowed === false && policy.matched_domain)%0A+     return 'This email domain is already blocked.';%0A+   return null;%0A+ }%0A+ function domainPolicyBlocksCheckbox(policy: DomainPolicyStatus | undefined) {%0A+   return (%0A+     !policy ||%0A+     Boolean(policy.error) ||%0A+     (policy.matched_domain !== null &&%0A+       (policy.allowed === true || policy.allowed === false))%0A+   );%0A+ }%0A+ function UserSuspensionControls({%0A+   user,%0A+   currentAdminId,%0A+   domainPolicy,%0A+ }: {%0A+   user: ResourceMembership;%0A+   currentAdminId: string;%0A+   domainPolicy?: DomainPolicyStatus;%0A+ }) {%0A+   const router = useRouter();%0A+   const [open, setOpen] = useState(false);%0A+   const [reason, setReason] = useState('');%0A+   const [blockDomain, setBlockDomain] = useState(false);%0A+   const [busy, setBusy] = useState(false);%0A+   const domain = domainFromEmail(user.email);%0A+   const checkboxDisabled = domainPolicyBlocksCheckbox(domainPolicy);%0A+   const policyMessage = domainPolicyMessage(domainPolicy);%0A+   const canSuspend =%0A+     user.id !== currentAdminId && user.role !== 'admin' && !user.is_suspended;%0A+   const canUnsuspend = user.role !== 'admin' && user.is_suspended;%0A+   async function patchUser(payload: Record<string, unknown>) {%0A+     setBusy(true);%0A+     try {%0A+       const response = await fetch(`/api/admin/users/${user.id}/suspension`, {%0A+         method: 'PATCH',%0A+         headers: { 'Content-Type': 'application/json' },%0A+         body: JSON.stringify(payload),%0A+       });%0A+       const result = await response.json().catch(() => ({}));%0A+       if (!response.ok)%0A+         throw new Error(%0A+           typeof result.error === 'string'%0A+             ? result.error%0A+             : 'Could not update suspension.',%0A+         );%0A+       toast.success(%0A+         payload.suspended === false ? 'User unsuspended.' : 'User suspended.',%0A+       );%0A+       if (Array.isArray(result.warnings))%0A+         result.warnings.forEach(%0A+           (warning: unknown) =>%0A+             typeof warning === 'string' && toast.warning(warning),%0A+         );%0A+       setOpen(false);%0A+       setReason('');%0A+       setBlockDomain(false);%0A+       router.refresh();%0A+     } catch (error) {%0A+       toast.error(%0A+         error instanceof Error ? error.message : 'Could not update suspension.',%0A+       );%0A+     } finally {%0A+       setBusy(false);%0A+     }%0A+   }%0A+   return (%0A+     <div className="min-w-64 space-y-2">%0A+       <div className="flex flex-wrap items-center gap-2">%0A+         <span%0A+           className={`rounded border px-2 py-0.5 text-xs font-medium ${user.is_suspended ? 'border-red-200 bg-red-50 text-red-700' : 'border-emerald-200 bg-emerald-50 text-emerald-700'}`}%0A+         >%0A+           {user.is_suspended ? 'Suspended' : 'Active'}%0A+         </span>%0A+         {canSuspend ? (%0A+           <button%0A+             className="rounded border border-red-200 px-2 py-1 text-xs font-medium text-red-700 hover:bg-red-50"%0A+             onClick={() => setOpen(true)}%0A+           >%0A+             Suspend%0A+           </button>%0A+         ) : null}%0A+         {canUnsuspend ? (%0A+           <button%0A+             className="rounded border border-emerald-200 px-2 py-1 text-xs font-medium text-emerald-700 hover:bg-emerald-50 disabled:opacity-60"%0A+             disabled={busy}%0A+             onClick={() => {%0A+               if (confirm(`Unsuspend ${user.email}?`))%0A+                 void patchUser({ suspended: false });%0A+             }}%0A+           >%0A+             Unsuspend%0A+           </button>%0A+         ) : null}%0A+       </div>%0A+       {user.suspended_at ? (%0A+         <p className="text-xs text-slate-500">%0A+           Suspended {new Date(user.suspended_at).toLocaleString()}%0A+         </p>%0A+       ) : null}%0A+       {user.suspension_reason ? (%0A+         <p className="text-xs text-red-700">Reason: {user.suspension_reason}</p>%0A+       ) : null}%0A+       {open ? (%0A+         <form%0A+           className="space-y-2 rounded bg-slate-50 p-2"%0A+           onSubmit={(event) => {%0A+             event.preventDefault();%0A+             void patchUser({%0A+               suspended: true,%0A+               reason,%0A+               blockDomain: blockDomain && !checkboxDisabled,%0A+             });%0A+           }}%0A+         >%0A+           <label className="block text-xs font-semibold text-slate-600">%0A+             Suspension reason%0A+             <textarea%0A+               className={`${inputClass} min-h-20 py-2`}%0A+               value={reason}%0A+               minLength={3}%0A+               maxLength={500}%0A+               required%0A+               onChange={(event) => setReason(event.target.value)}%0A+             />%0A+           </label>%0A+           <label className="flex items-center gap-2 text-xs text-slate-600">%0A+             <input%0A+               type="checkbox"%0A+               checked={blockDomain && !checkboxDisabled}%0A+               disabled={checkboxDisabled}%0A+               onChange={(event) => setBlockDomain(event.target.checked)}%0A+             />%0A+             Also block this email domain%0A+           </label>%0A+           {domain && policyMessage ? (%0A+             <p className="text-xs text-amber-700">{policyMessage}</p>%0A+           ) : domain ? (%0A+             <p className="text-xs text-slate-500">%0A+               The server will preserve any explicit allow rule for {domain}.%0A+             </p>%0A+           ) : null}%0A+           <div className="flex gap-2">%0A+             <button%0A+               className="rounded bg-red-700 px-2 py-1 text-xs font-medium text-white disabled:opacity-60"%0A+               disabled={busy}%0A+             >%0A+               Confirm suspension%0A+             </button>%0A+             <button%0A+               type="button"%0A+               className="rounded border px-2 py-1 text-xs"%0A+               onClick={() => setOpen(false)}%0A+             >%0A+               Cancel%0A+             </button>%0A+           </div>%0A+         </form>%0A+       ) : null}%0A+     </div>%0A+   );%0A+ }%0A+%0A+ export function AdminConsole({%0A+   currentAdminId,%0A+   sp,%0A+   reports,%0A+   tickets,%0A+   memberships,%0A+   domainPolicies = {},%0A+   logs,%0A+   usage = [],%0A+   usageResource = null,%0A+   usageUsers = [],%0A+   usageUserResources = [],%0A+   usageSelectedUser = null,%0A+   diagnostics = [],%0A+   counts,%0A+   pages,%0A+   sizes,%0A+   indexPanel,%0A+   configuredWarnings,%0A+   exportQuery,%0A+ }: {%0A+   sp: Record<string, string | undefined>;%0A+   reports: ResourceReport[];%0A+   tickets: SupportTicket[];%0A+   memberships: ResourceMembership[];%0A+   domainPolicies?: Record<string, DomainPolicyStatus>;%0A+   logs: ActivityLog[];%0A+   usage?: any[];%0A+   usageResource?: any;%0A+   usageUsers?: any[];%0A+   usageUserResources?: any[];%0A+   usageSelectedUser?: ResourceMembership | null;%0A+   diagnostics?: any[];%0A+   counts: any;%0A+   pages: any;%0A+   sizes: any;%0A+   indexPanel: React.ReactNode;%0A+   configuredWarnings: React.ReactNode;%0A+   exportQuery: string;%0A+   currentAdminId: string;%0A+ }) {%0A+   const section = sp.section || 'index';%0A+   const notificationFeed = useNotificationFeed(true);%0A+   const [selected, setSelected] = useState<{%0A+     kind: 'report' | 'ticket';%0A+     item: any;%0A+   } | null>(null);%0A+   const admins = useMemo(%0A+     () => memberships.filter((m) => m.role === 'admin'),%0A+     [memberships],%0A+   );%0A+   useEffect(() => {%0A+     if (section === 'reports') void markNotificationCategory('admin_reports');%0A+     if (section === 'tickets') void markNotificationCategory('admin_tickets');%0A+   }, [section]);%0A+   useEffect(() => {%0A+     const selectedId =%0A+       section === 'reports'%0A+         ? sp.reportId%0A+         : section === 'tickets'%0A+           ? sp.ticketId%0A+           : null;%0A+     if (!selectedId) return;%0A+     const rows = section === 'reports' ? reports : tickets;%0A+     const item = rows.find((row) => row.id === selectedId);%0A+     if (item)%0A+       setSelected({ kind: section === 'reports' ? 'report' : 'ticket', item });%0A+   }, [reports, section, sp.reportId, sp.ticketId, tickets]);%0A+   const tabs: Array<[string, string, number]> = [%0A+     ['index', 'Library index', 0],%0A+     ['question-bank', 'Question bank', 0],%0A+     ['reports', 'Resource reports', notificationFeed.summary.adminReports],%0A+     ['tickets', 'Support tickets', notificationFeed.summary.adminTickets],%0A+     ['users', 'Users', 0],%0A+     ['activity', 'Activity', 0],%0A+     ['analytics', 'Usage analytics', 0],%0A+     ['diagnostics', 'Diagnostics', 0],%0A+   ];%0A+   const activityLabel = (a: string) =>%0A+     ({%0A+       folder_opened: 'Opened folder',%0A+       file_opened: 'Opened file',%0A+       download_started: 'Started download',%0A+     })[a] || a;%0A+   return (%0A+     <>%0A+       <nav%0A+         className="mt-4 flex flex-wrap gap-2 border-b border-slate-200 text-sm"%0A+         aria-label="Admin sections"%0A+       >%0A+         {tabs.map(([id, label, unread]) => (%0A+           <Link%0A+             key={id}%0A+             href={id === 'question-bank' ? '/admin/question-bank' : sectionHref(sp, id)}%0A+             className={`flex items-center gap-1.5 rounded-t-md px-3 py-2 ${section === id ? 'bg-slate-100 font-semibold text-[color:var(--dp-navy)]' : 'text-slate-600 hover:bg-slate-50'}`}%0A+           >%0A+             {label}%0A+             <NotificationBadge count={unread} />%0A+           </Link>%0A+         ))}%0A+       </nav>%0A+       {configuredWarnings}%0A+       {section === 'index' && <section className="mt-5">{indexPanel}</section>}%0A+       {section === 'reports' && (%0A+         <section className="mt-5">%0A+           <h2 className="text-base font-semibold">Resource reports</h2>%0A+           <LiveFilters prefix="report" sp={sp} section="reports" />%0A+           <QueueTable%0A+             rows={reports}%0A+             kind="report"%0A+             onInspect={(item) => setSelected({ kind: 'report', item })}%0A+           />%0A+           {pager(sp, 'reportPage', pages.report, counts.report, sizes.page)}%0A+         </section>%0A+       )}%0A+       {section === 'tickets' && (%0A+         <section className="mt-5">%0A+           <h2 className="text-base font-semibold">Support tickets</h2>%0A+           <LiveFilters prefix="ticket" sp={sp} section="tickets" />%0A+           <QueueTable%0A+             rows={tickets}%0A+             kind="ticket"%0A+             onInspect={(item) => setSelected({ kind: 'ticket', item })}%0A+           />%0A+           {pager(sp, 'ticketPage', pages.ticket, counts.ticket, sizes.page)}%0A+         </section>%0A+       )}%0A+       {section === 'users' && (%0A+         <section className="mt-5">%0A+           <h2 className="text-base font-semibold">Users</h2>%0A+           <UserFilters sp={sp} sizes={sizes} />%0A+           <div className="mt-3 overflow-x-auto border-y border-slate-200 bg-white">%0A+             <table className="min-w-full text-sm">%0A+               <thead className="bg-slate-50 text-left text-xs uppercase text-slate-500">%0A+                 <tr>%0A+                   <th className="p-2">Full name</th>%0A+                   <th className="p-2">Username</th>%0A+                   <th className="p-2">Email</th>%0A+                   <th className="p-2">Role</th>%0A+                   <th className="p-2">Joined</th>%0A+                   <th className="p-2">Last activity</th>%0A+                   <th className="p-2">Suspension</th>%0A+                 </tr>%0A+               </thead>%0A+               <tbody>%0A+                 {memberships.map((u) => (%0A+                   <tr key={u.id} className="border-t border-slate-100">%0A+                     <td className="whitespace-nowrap p-2 font-medium">%0A+                       <Link%0A+                         className="text-[color:var(--dp-blue)] hover:underline"%0A+                         href={{%0A+                           query: {%0A+                             ...sp,%0A+                             section: 'users',%0A+                             userUsageId: u.id,%0A+                             userUsageRange: sp.userUsageRange || 'all',%0A+                           },%0A+                         }}%0A+                       >%0A+                         {u.full_name || '—'}%0A+                       </Link>%0A+                     </td>%0A+                     <td className="whitespace-nowrap p-2 font-medium">%0A+                       <Link%0A+                         className="text-[color:var(--dp-blue)] hover:underline"%0A+                         href={{%0A+                           query: {%0A+                             ...sp,%0A+                             section: 'users',%0A+                             userUsageId: u.id,%0A+                             userUsageRange: sp.userUsageRange || 'all',%0A+                           },%0A+                         }}%0A+                       >%0A+                         {u.username ? `@${u.username}` : '—'}%0A+                       </Link>%0A+                     </td>%0A+                     <td className="p-2 font-medium">%0A+                       <Link%0A+                         className="text-[color:var(--dp-blue)] hover:underline"%0A+                         href={{%0A+                           query: {%0A+                             ...sp,%0A+                             section: 'users',%0A+                             userUsageId: u.id,%0A+                             userUsageRange: sp.userUsageRange || 'all',%0A+                           },%0A+                         }}%0A+                       >%0A+                         {u.email}%0A+                       </Link>%0A+                     </td>%0A+                     <td className="p-2">{u.role}</td>%0A+                     <td className="p-2 text-slate-500">%0A+                       {new Date(u.created_at).toLocaleDateString()}%0A+                     </td>%0A+                     <td className="p-2 text-slate-500">%0A+                       {(u as any).latest_activity_at%0A+                         ? new Date(%0A+                             (u as any).latest_activity_at,%0A+                           ).toLocaleString()%0A+                         : '—'}%0A+                     </td>%0A+                     <td className="p-2 align-top">%0A+                       <UserSuspensionControls%0A+                         user={u}%0A+                         currentAdminId={currentAdminId}%0A+                         domainPolicy={domainPolicies[domainFromEmail(u.email)]}%0A+                       />%0A+                     </td>%0A+                   </tr>%0A+                 ))}%0A+               </tbody>%0A+             </table>%0A+             {pager(sp, 'userPage', pages.user, counts.user, sizes.user)}%0A+           </div>%0A+         </section>%0A+       )}%0A+       {section === 'activity' && (%0A+         <section className="mt-5">%0A+           <h2 className="text-base font-semibold">Activity</h2>%0A+           <ActivityFilters sp={sp} />%0A+           <a%0A+             className="mt-3 inline-block text-sm font-medium text-[color:var(--dp-blue)]"%0A+             href={`/api/admin/activity/export?${exportQuery}`}%0A+           >%0A+             Export CSV%0A+           </a>%0A+           <div className="mt-3 overflow-x-auto border-y border-slate-200 bg-white">%0A+             <table className="min-w-full text-sm">%0A+               <thead className="bg-slate-50 text-left text-xs uppercase text-slate-500">%0A+                 <tr>%0A+                   <th className="p-2">Time</th>%0A+                   <th className="p-2">User</th>%0A+                   <th className="p-2">Activity</th>%0A+                   <th className="p-2">Resource</th>%0A+                 </tr>%0A+               </thead>%0A+               <tbody>%0A+                 {logs.map((log) => (%0A+                   <tr key={log.id} className="border-t border-slate-100">%0A+                     <td className="p-2 text-slate-500">%0A+                       {new Date(log.created_at).toLocaleString()}%0A+                     </td>%0A+                     <td className="p-2 font-medium">{log.user_email}</td>%0A+                     <td className="p-2">{activityLabel(log.action)}</td>%0A+                     <td className="p-2">{log.file_name || 'Resource'}</td>%0A+                   </tr>%0A+                 ))}%0A+               </tbody>%0A+             </table>%0A+             {pager(sp, 'activityPage', pages.activity, counts.activity, 50)}%0A+           </div>%0A+         </section>%0A+       )}%0A+       {section === 'analytics' && (%0A+         <section className="mt-5">%0A+           <div className="flex flex-wrap items-center justify-between gap-3">%0A+             <div>%0A+               <h2 className="text-base font-semibold">%0A+                 Resource usage leaderboard%0A+               </h2>%0A+               <p className="text-sm text-slate-600">%0A+                 Active viewing time only; not keystrokes, screen recording, or%0A+                 perfect study time.%0A+               </p>%0A+             </div>%0A+             <div className="flex gap-2">%0A+               {['today', '7d', '30d', 'all'].map((r) => (%0A+                 <Link%0A+                   key={r}%0A+                   href={{ query: { ...sp, section: 'analytics', range: r } }}%0A+                   className={secondaryBtn}%0A+                 >%0A+                   {r}%0A+                 </Link>%0A+               ))}%0A+             </div>%0A+           </div>%0A+           <div className="mt-3 overflow-x-auto border-y border-slate-200 bg-white">%0A+             <table className="min-w-full text-sm">%0A+               <thead className="bg-slate-50 text-left text-xs uppercase text-slate-500">%0A+                 <tr>%0A+                   <th className="p-2">Rank</th>%0A+                   <th className="p-2">Resource name</th>%0A+                   <th className="p-2">Subject/path</th>%0A+                   <th className="p-2">File type</th>%0A+                   <th className="p-2">Total active time</th>%0A+                   <th className="p-2">Unique users</th>%0A+                   <th className="p-2">Sessions</th>%0A+                   <th className="p-2">Average/session</th>%0A+                   <th className="p-2">Last used</th>%0A+                   <th className="p-2">Actions</th>%0A+                 </tr>%0A+               </thead>%0A+               <tbody>%0A+                 {usage.length ? (%0A+                   usage.map((r: any) => (%0A+                     <tr key={r.file_id} className="border-t border-slate-100">%0A+                       <td className="p-2 font-semibold">{r.rank}</td>%0A+                       <td className="p-2 font-medium">%0A+                         <Link%0A+                           className="text-[color:var(--dp-blue)]"%0A+                           href={{%0A+                             query: {%0A+                               ...sp,%0A+                               section: 'analytics',%0A+                               range: sp.range || '30d',%0A+                               resourceId: r.file_id,%0A+                             },%0A+                           }}%0A+                         >%0A+                           {r.resource_name}%0A+                         </Link>%0A+                       </td>%0A+                       <td className="p-2 text-slate-600">{r.resource_path}</td>%0A+                       <td className="whitespace-nowrap p-2">%0A+                         {formatMimeType(r.mime_type, r.resource_name)}%0A+                       </td>%0A+                       <td className="p-2">%0A+                         {fmtSeconds(Number(r.total_active_seconds))}%0A+                       </td>%0A+                       <td className="p-2">{r.unique_users}</td>%0A+                       <td className="p-2">{r.session_count}</td>%0A+                       <td className="p-2">%0A+                         {fmtSeconds(Number(r.average_seconds_per_session))}%0A+                       </td>%0A+                       <td className="whitespace-nowrap p-2">%0A+                         {r.last_used_at%0A+                           ? new Date(r.last_used_at).toLocaleString()%0A+                           : '—'}%0A+                       </td>%0A+                       <td className="p-2">%0A+                         <div className="flex min-w-36 flex-wrap gap-2">%0A+                           <Link%0A+                             className="font-medium text-[color:var(--dp-blue)]"%0A+                             target="_blank"%0A+                             rel="noreferrer"%0A+                             href={`/resource/${r.file_id}`}%0A+                           >%0A+                             Open preview%0A+                           </Link>%0A+                           <Link%0A+                             className="font-medium text-[color:var(--dp-blue)]"%0A+                             href={{%0A+                               query: {%0A+                                 ...sp,%0A+                                 section: 'analytics',%0A+                                 range: sp.range || '30d',%0A+                                 resourceId: r.file_id,%0A+                               },%0A+                             }}%0A+                           >%0A+                             View stats%0A+                           </Link>%0A+                         </div>%0A+                       </td>%0A+                     </tr>%0A+                   ))%0A+                 ) : (%0A+                   <tr>%0A+                     <td%0A+                       colSpan={10}%0A+                       className="p-6 text-center text-sm text-slate-600"%0A+                     >%0A+                       No usage data yet. Open a resource as an approved user and%0A+                       wait at least one heartbeat interval.%0A+                     </td>%0A+                   </tr>%0A+                 )}%0A+               </tbody>%0A+             </table>%0A+           </div>%0A+           {usageResource && (%0A+             <ResourceUsageModal%0A+               sp={sp}%0A+               usageResource={usageResource}%0A+               usageUsers={usageUsers}%0A+               usageUserResources={usageUserResources}%0A+             />%0A+           )}%0A+         </section>%0A+       )}%0A+       {section === 'diagnostics' && (%0A+         <section className="mt-5">%0A+           <h2 className="text-base font-semibold">Production diagnostics</h2>%0A+           <p className="text-sm text-slate-600">%0A+             Admin-only sanitized recent server errors.%0A+           </p>%0A+           <div className="mt-3 overflow-x-auto border-y border-slate-200 bg-white">%0A+             <table className="min-w-full text-sm">%0A+               <tbody>%0A+                 {diagnostics.map((d: any, i: number) => (%0A+                   <tr key={i} className="border-t border-slate-100">%0A+                     <td className="p-2 text-slate-500">%0A+                       {new Date(d.occurred_at).toLocaleString()}%0A+                     </td>%0A+                     <td className="p-2 font-medium">{d.area}</td>%0A+                     <td className="p-2">{d.message}</td>%0A+                   </tr>%0A+                 ))}%0A+               </tbody>%0A+             </table>%0A+           </div>%0A+         </section>%0A+       )}%0A+       {selected && (%0A+         <CaseInspector%0A+           kind={selected.kind}%0A+           item={selected.item}%0A+           admins={admins}%0A+           onClose={() => setSelected(null)}%0A+         />%0A+       )}%0A+       {section === 'users' && usageSelectedUser && (%0A+         <UserUsageModal%0A+           sp={sp}%0A+           user={usageSelectedUser}%0A+           resources={usageUserResources}%0A+         />%0A+       )}%0A+     </>%0A+   );%0A+ }%0A+%0A+ function UserUsageModal({%0A+   sp,%0A+   user,%0A+   resources,%0A+ }: {%0A+   sp: Record<string, string | undefined>;%0A+   user: ResourceMembership;%0A+   resources: any[];%0A+ }) {%0A+   const router = useRouter();%0A+   const pathname = usePathname();%0A+   const closeQuery = {%0A+     ...sp,%0A+     section: 'users',%0A+     userUsageId: undefined,%0A+     userUsageRange: undefined,%0A+   };%0A+   const close = () => {%0A+     const query = new URLSearchParams();%0A+     Object.entries(closeQuery).forEach(([key, value]) => {%0A+       if (value) query.set(key, value);%0A+     });%0A+     const suffix = query.toString();%0A+     router.replace(suffix ? `${pathname}?${suffix}` : pathname);%0A+   };%0A+   const totalActiveSeconds = resources.reduce(%0A+     (total, resource) => total + Number(resource.total_active_seconds || 0),%0A+     0,%0A+   );%0A+   const totalSessions = resources.reduce(%0A+     (total, resource) => total + Number(resource.session_count || 0),%0A+     0,%0A+   );%0A+   const lastViewedAt = resources.reduce<string | null>(%0A+     (latest, resource) =>%0A+       !latest || (resource.last_used_at && resource.last_used_at > latest)%0A+         ? resource.last_used_at%0A+         : latest,%0A+     null,%0A+   );%0A+%0A+   useEffect(() => {%0A+     const onKey = (event: KeyboardEvent) => {%0A+       if (event.key === 'Escape') close();%0A+     };%0A+     window.addEventListener('keydown', onKey);%0A+     return () => window.removeEventListener('keydown', onKey);%0A+   });%0A+%0A+   return (%0A+     <div%0A+       className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-3 sm:p-6"%0A+       role="dialog"%0A+       aria-modal="true"%0A+       aria-labelledby="user-usage-modal-title"%0A+       onMouseDown={(event) => {%0A+         if (event.target === event.currentTarget) close();%0A+       }}%0A+     >%0A+       <div className="flex max-h-[90vh] w-full max-w-5xl flex-col overflow-hidden rounded-xl border border-slate-200 bg-white shadow-2xl">%0A+         <header className="sticky top-0 z-10 flex items-start justify-between gap-3 border-b border-slate-200 bg-white p-4 shadow-sm">%0A+           <div>%0A+             <h3%0A+               id="user-usage-modal-title"%0A+               className="text-lg font-semibold text-[color:var(--dp-navy)]"%0A+             >%0A+               Resource analytics for{' '}%0A+               {user.username ? `@${user.username}` : user.email}%0A+             </h3>%0A+             <p className="mt-1 text-sm text-slate-600">%0A+               {[user.full_name, user.email].filter(Boolean).join(' · ')}%0A+             </p>%0A+           </div>%0A+           <CloseButton label="Close user resource analytics" onClick={close} />%0A+         </header>%0A+         <div className="max-h-[calc(90vh-8rem)] overflow-y-auto p-4">%0A+           <div className="flex flex-wrap gap-2">%0A+             {[%0A+               ['today', 'Today'],%0A+               ['7d', '7 days'],%0A+               ['30d', '30 days'],%0A+               ['all', 'All time'],%0A+             ].map(([range, label]) => (%0A+               <Link%0A+                 key={range}%0A+                 className={`${secondaryBtn} ${(sp.userUsageRange || 'all') === range ? 'border-[color:var(--dp-blue)] text-[color:var(--dp-blue)]' : ''}`}%0A+                 href={{%0A+                   query: {%0A+                     ...sp,%0A+                     section: 'users',%0A+                     userUsageId: user.id,%0A+                     userUsageRange: range,%0A+                   },%0A+                 }}%0A+               >%0A+                 {label}%0A+               </Link>%0A+             ))}%0A+           </div>%0A+           <dl className="mt-4 grid gap-2 rounded-lg border border-slate-200 bg-slate-50 p-3 text-xs sm:grid-cols-4">%0A+             <div>%0A+               <dt className="text-slate-500">Files viewed</dt>%0A+               <dd className="font-semibold">{resources.length}</dd>%0A+             </div>%0A+             <div>%0A+               <dt className="text-slate-500">Total active time</dt>%0A+               <dd className="font-semibold">%0A+                 {fmtSeconds(totalActiveSeconds)}%0A+               </dd>%0A+             </div>%0A+             <div>%0A+               <dt className="text-slate-500">Sessions</dt>%0A+               <dd className="font-semibold">{totalSessions}</dd>%0A+             </div>%0A+             <div>%0A+               <dt className="text-slate-500">Last viewed</dt>%0A+               <dd className="font-semibold">%0A+                 {lastViewedAt ? new Date(lastViewedAt).toLocaleString() : '—'}%0A+               </dd>%0A+             </div>%0A+           </dl>%0A+           <div className="mt-4 overflow-x-auto rounded-lg border border-slate-200">%0A+             {resources.length ? (%0A+               <table className="min-w-full text-sm">%0A+                 <thead className="sticky top-0 bg-slate-50 text-left text-xs uppercase text-slate-500">%0A+                   <tr>%0A+                     <th className="p-2">Resource</th>%0A+                     <th className="p-2">Subject/path</th>%0A+                     <th className="p-2">Active time</th>%0A+                     <th className="p-2">Sessions</th>%0A+                     <th className="p-2">Last viewed</th>%0A+                     <th className="p-2">Action</th>%0A+                   </tr>%0A+                 </thead>%0A+                 <tbody>%0A+                   {resources.map((resource) => (%0A+                     <tr%0A+                       key={resource.file_id}%0A+                       className="border-t border-slate-100"%0A+                     >%0A+                       <td className="p-2 font-medium">%0A+                         {resource.resource_name}%0A+                       </td>%0A+                       <td className="p-2 text-slate-600">%0A+                         {resource.resource_path || '—'}%0A+                       </td>%0A+                       <td className="whitespace-nowrap p-2">%0A+                         {fmtSeconds(Number(resource.total_active_seconds))}%0A+                       </td>%0A+                       <td className="p-2">{resource.session_count}</td>%0A+                       <td className="whitespace-nowrap p-2">%0A+                         {resource.last_used_at%0A+                           ? new Date(resource.last_used_at).toLocaleString()%0A+                           : '—'}%0A+                       </td>%0A+                       <td className="p-2">%0A+                         <Link%0A+                           className="font-medium text-[color:var(--dp-blue)]"%0A+                           href={`/resource/${resource.file_id}`}%0A+                           target="_blank"%0A+                           rel="noreferrer"%0A+                         >%0A+                           Open preview%0A+                         </Link>%0A+                       </td>%0A+                     </tr>%0A+                   ))}%0A+                 </tbody>%0A+               </table>%0A+             ) : (%0A+               <p className="rounded-md border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-600">%0A+                 No resource views recorded for this user in this range.%0A+               </p>%0A+             )}%0A+           </div>%0A+         </div>%0A+       </div>%0A+     </div>%0A+   );%0A+ }%0A+%0A+ function ResourceUsageModal({%0A+   sp,%0A+   usageResource,%0A+   usageUsers,%0A+   usageUserResources,%0A+ }: {%0A+   sp: Record<string, string | undefined>;%0A+   usageResource: any;%0A+   usageUsers: any[];%0A+   usageUserResources: any[];%0A+ }) {%0A+   const router = useRouter();%0A+   const pathname = usePathname();%0A+   const selectedUser = usageUsers.find((u: any) => u.user_id === sp.userId);%0A+   const closeQuery = {%0A+     ...sp,%0A+     section: 'analytics',%0A+     resourceId: undefined,%0A+     userId: undefined,%0A+   };%0A+   const clearUserQuery = {%0A+     ...sp,%0A+     section: 'analytics',%0A+     resourceId: usageResource.file_id,%0A+     userId: undefined,%0A+   };%0A+   useEffect(() => {%0A+     const onKey = (event: KeyboardEvent) => {%0A+       if (event.key === 'Escape') {%0A+         const q = new URLSearchParams();%0A+         Object.entries(closeQuery).forEach(([k, v]) => {%0A+           if (v) q.set(k, v);%0A+         });%0A+         router.replace(`${pathname}?${q.toString()}`);%0A+       }%0A+     };%0A+     window.addEventListener('keydown', onKey);%0A+     return () => window.removeEventListener('keydown', onKey);%0A+   }, [router, pathname, sp.section, sp.range, sp.resourceId, sp.userId]);%0A+   return (%0A+     <div%0A+       className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/50 p-3 sm:p-6"%0A+       role="dialog"%0A+       aria-modal="true"%0A+       aria-labelledby="resource-usage-modal-title"%0A+       onMouseDown={(e) => {%0A+         if (e.target === e.currentTarget) {%0A+           const q = new URLSearchParams();%0A+           Object.entries(closeQuery).forEach(([k, v]) => {%0A+             if (v) q.set(k, v);%0A+           });%0A+           router.replace(`${pathname}?${q.toString()}`);%0A+         }%0A+       }}%0A+     >%0A+       <div className="flex max-h-[90vh] w-full max-w-6xl flex-col overflow-hidden rounded-xl border border-slate-200 bg-white shadow-2xl">%0A+         <header className="sticky top-0 z-10 flex flex-wrap items-start justify-between gap-3 border-b border-slate-200 bg-white p-4 shadow-sm">%0A+           <div>%0A+             <h3%0A+               id="resource-usage-modal-title"%0A+               className="text-lg font-semibold text-[color:var(--dp-navy)]"%0A+             >%0A+               Resource usage leaderboard%0A+             </h3>%0A+             <p className="mt-1 text-sm font-medium text-slate-700">%0A+               {usageResource.resource_name}%0A+             </p>%0A+             <p className="text-xs font-medium text-slate-500">%0A+               {formatMimeType(%0A+                 usageResource.mime_type,%0A+                 usageResource.resource_name,%0A+               )}%0A+             </p>%0A+             <p className="text-xs text-slate-500">%0A+               {usageResource.resource_path}%0A+             </p>%0A+           </div>%0A+           <div className="flex flex-wrap items-center gap-2">%0A+             <Link%0A+               className={secondaryBtn}%0A+               target="_blank"%0A+               rel="noreferrer"%0A+               href={`/resource/${usageResource.file_id}`}%0A+             >%0A+               Open preview%0A+             </Link>%0A+             <CloseButton%0A+               label="Close resource usage stats"%0A+               onClick={() => {%0A+                 const q = new URLSearchParams();%0A+                 Object.entries(closeQuery).forEach(([k, v]) => {%0A+                   if (v) q.set(k, v);%0A+                 });%0A+                 router.replace(`${pathname}?${q.toString()}`);%0A+               }}%0A+             />%0A+           </div>%0A+         </header>%0A+         <div className="max-h-[calc(90vh-8rem)] overflow-y-auto p-4">%0A+           <dl className="grid gap-2 rounded-lg border border-slate-200 bg-slate-50 p-3 text-xs sm:grid-cols-5">%0A+             <div>%0A+               <dt className="text-slate-500">Current rank</dt>%0A+               <dd className="font-semibold">#{usageResource.rank}</dd>%0A+             </div>%0A+             <div>%0A+               <dt className="text-slate-500">Total active time</dt>%0A+               <dd className="font-semibold">%0A+                 {fmtSeconds(Number(usageResource.total_active_seconds))}%0A+               </dd>%0A+             </div>%0A+             <div>%0A+               <dt className="text-slate-500">Unique users</dt>%0A+               <dd className="font-semibold">{usageResource.unique_users}</dd>%0A+             </div>%0A+             <div>%0A+               <dt className="text-slate-500">Sessions</dt>%0A+               <dd className="font-semibold">{usageResource.session_count}</dd>%0A+             </div>%0A+             <div>%0A+               <dt className="text-slate-500">Last used</dt>%0A+               <dd className="font-semibold">%0A+                 {usageResource.last_used_at%0A+                   ? new Date(usageResource.last_used_at).toLocaleString()%0A+                   : '—'}%0A+               </dd>%0A+             </div>%0A+           </dl>%0A+           <h4 className="mt-4 text-sm font-semibold">%0A+             Per-resource user breakdown%0A+           </h4>%0A+           <div className="mt-2 overflow-x-auto rounded-lg border border-slate-200">%0A+             {usageUsers.length ? (%0A+               <table className="min-w-full text-sm">%0A+                 <thead className="sticky top-0 bg-slate-50 text-left text-xs uppercase text-slate-500">%0A+                   <tr>%0A+                     <th className="p-2">Rank</th>%0A+                     <th className="p-2">User email</th>%0A+                     <th className="p-2">Total active time</th>%0A+                     <th className="p-2">Sessions</th>%0A+                     <th className="p-2">Last used</th>%0A+                     <th className="p-2">Action</th>%0A+                   </tr>%0A+                 </thead>%0A+                 <tbody>%0A+                   {usageUsers.map((u: any, i: number) => (%0A+                     <tr key={u.user_id} className="border-t border-slate-100">%0A+                       <td className="p-2 font-semibold">{i + 1}</td>%0A+                       <td className="p-2 font-medium">{u.user_email}</td>%0A+                       <td className="p-2">%0A+                         {fmtSeconds(Number(u.total_active_seconds))}%0A+                       </td>%0A+                       <td className="p-2">{u.session_count}</td>%0A+                       <td className="whitespace-nowrap p-2">%0A+                         {u.last_used_at%0A+                           ? new Date(u.last_used_at).toLocaleString()%0A+                           : '—'}%0A+                       </td>%0A+                       <td className="p-2">%0A+                         <Link%0A+                           className="font-medium text-[color:var(--dp-blue)]"%0A+                           href={{%0A+                             query: {%0A+                               ...sp,%0A+                               section: 'analytics',%0A+                               resourceId: usageResource.file_id,%0A+                               userId: u.user_id,%0A+                             },%0A+                           }}%0A+                         >%0A+                           View user usage%0A+                         </Link>%0A+                       </td>%0A+                     </tr>%0A+                   ))}%0A+                 </tbody>%0A+               </table>%0A+             ) : (%0A+               <p className="rounded-md border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-600">%0A+                 No user breakdown available for this range.%0A+               </p>%0A+             )}%0A+           </div>%0A+           {sp.userId && (%0A+             <section className="mt-4 rounded-lg border border-slate-200 bg-white p-4">%0A+               <div className="flex flex-wrap items-center justify-between gap-2">%0A+                 <h4 className="font-semibold text-[color:var(--dp-navy)]">%0A+                   Top resources used by{' '}%0A+                   {selectedUser?.user_email || 'selected user'}%0A+                 </h4>%0A+                 <Link className={secondaryBtn} href={{ query: clearUserQuery }}>%0A+                   Clear user%0A+                 </Link>%0A+               </div>%0A+               {usageUserResources.length ? (%0A+                 <div className="mt-2 overflow-x-auto">%0A+                   <table className="min-w-full text-sm">%0A+                     <thead className="bg-slate-50 text-left text-xs uppercase text-slate-500">%0A+                       <tr>%0A+                         <th className="p-2">Resource name</th>%0A+                         <th className="p-2">Total active time</th>%0A+                         <th className="p-2">Sessions</th>%0A+                         <th className="p-2">Last used</th>%0A+                       </tr>%0A+                     </thead>%0A+                     <tbody>%0A+                       {usageUserResources.map((r: any) => (%0A+                         <tr%0A+                           key={r.file_id}%0A+                           className="border-t border-slate-100"%0A+                         >%0A+                           <td className="p-2 font-medium">{r.resource_name}</td>%0A+                           <td className="p-2">%0A+                             {fmtSeconds(Number(r.total_active_seconds))}%0A+                           </td>%0A+                           <td className="p-2">{r.session_count}</td>%0A+                           <td className="whitespace-nowrap p-2">%0A+                             {r.last_used_at%0A+                               ? new Date(r.last_used_at).toLocaleString()%0A+                               : '—'}%0A+                           </td>%0A+                         </tr>%0A+                       ))}%0A+                     </tbody>%0A+                   </table>%0A+                 </div>%0A+               ) : (%0A+                 <p className="mt-2 rounded-md border border-dashed border-slate-200 bg-slate-50 p-4 text-sm text-slate-600">%0A+                   No usage data available for this user in this range.%0A+                 </p>%0A+               )}%0A+             </section>%0A+           )}%0A+         </div>%0A+       </div>%0A+     </div>%0A+   );%0A+ }%0A+ function QueueTable({%0A+   rows,%0A+   kind,%0A+   onInspect,%0A+ }: {%0A+   rows: any[];%0A+   kind: 'report' | 'ticket';%0A+   onInspect: (item: any) => void;%0A+ }) {%0A+   return (%0A+     <div className="overflow-x-auto border-y border-slate-200 bg-white">%0A+       <table className="min-w-full text-sm">%0A+         <thead className="bg-slate-50 text-left text-xs uppercase text-slate-500">%0A+           <tr>%0A+             <th className="p-2">Status</th>%0A+             <th className="p-2">Category</th>%0A+             <th className="p-2">%0A+               {kind === 'report' ? 'Resource' : 'Subject'}%0A+             </th>%0A+             <th className="p-2">Reporter</th>%0A+             <th className="p-2">Submitted</th>%0A+             <th className="p-2">Assigned</th>%0A+             <th className="p-2">Action</th>%0A+           </tr>%0A+         </thead>%0A+         <tbody>%0A+           {rows.map((r) => (%0A+             <tr%0A+               key={r.id}%0A+               onClick={() => onInspect(r)}%0A+               className="cursor-pointer border-t border-slate-100 hover:bg-slate-50"%0A+             >%0A+               <td className="p-2">{badge(r.status)}</td>%0A+               <td className="p-2">{r.category}</td>%0A+               <td className="max-w-72 truncate p-2">%0A+                 {kind === 'report' ? r.resource_name : r.subject}%0A+                 <p className="text-xs text-slate-500">%0A+                   {kind === 'report' ? r.resource_path : ''}%0A+                 </p>%0A+               </td>%0A+               <td className="p-2">{r.reporter_email}</td>%0A+               <td className="p-2">{new Date(r.created_at).toLocaleString()}</td>%0A+               <td className="p-2">%0A+                 {r.assigned_to ? 'Assigned' : 'Unassigned'}%0A+               </td>%0A+               <td className="p-2">%0A+                 <button%0A+                   onClick={(e) => {%0A+                     e.stopPropagation();%0A+                     onInspect(r);%0A+                   }}%0A+                   className="font-medium text-[color:var(--dp-blue)]"%0A+                 >%0A+                   Inspect%0A+                 </button>%0A+               </td>%0A+             </tr>%0A+           ))}%0A+         </tbody>%0A+       </table>%0A+     </div>%0A+   );%0A+ }%0A+%0A+  dp-source-text %0A%0A ❯ tests/suspension-ux-regression.test.ts:68:20%0A%0A

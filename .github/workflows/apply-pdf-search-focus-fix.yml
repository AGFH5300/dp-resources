name: Apply PDF search match focus fix

on:
  push:
    branches:
      - fix/pdf-search-focus-match

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/pdf-search-focus-match
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/apply-pdf-search-focus-fix.py
      - run: |
          rm scripts/apply-pdf-search-focus-fix.py
          rm .github/workflows/apply-pdf-search-focus-fix.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/resource/'[fileId]'/pdf-viewer.tsx tests/pdf-search-focus-match.test.ts scripts/apply-pdf-search-focus-fix.py .github/workflows/apply-pdf-search-focus-fix.yml
          git commit -m "Focus PDF search on exact highlighted matches"
          git push origin HEAD:fix/pdf-search-focus-match

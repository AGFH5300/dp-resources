name: Tutorial click cue position autofix

on:
  push:
    branches:
      - fix/tutorial-single-source-click-cue

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, '[tutorial-click-cue-position]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/tutorial-single-source-click-cue
          fetch-depth: 0
      - name: Apply click cue positioning polish
        run: python3 scripts/tmp-position-click-cue.py
      - name: Commit click cue positioning polish
        run: |
          if git diff --quiet; then
            echo "No patch changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add components/tutorial/tutorial-controller.tsx tests/tutorial-onboarding.test.ts
          git commit -m "Keep tutorial click beacon clear of tooltip [tutorial-click-cue-position]"
          git push origin HEAD:fix/tutorial-single-source-click-cue

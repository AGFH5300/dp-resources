name: Start guarded Exam-Mate recovery audit

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/trigger-exam-mate-production-import.yml

permissions:
  contents: read

jobs:
  marker:
    name: Recovery audit trigger marker
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Trusted-main recovery audit trigger: pinned R2 preflight.'

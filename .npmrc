# Native deployment platforms may install with NODE_ENV=production before running `npm run build`.
# The Next.js build requires the TypeScript and ESLint packages declared in devDependencies.
include=dev
